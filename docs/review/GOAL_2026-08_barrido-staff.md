# GOAL 2026-08 — Barrido de pantallas STAFF (coach / familia / admin)

Diagnóstico post-rebrand + rediseño. **Solo lectura**: no se tocó código.

- Rama: `feat/goal-extras`
- Servidor: `PORT=3026 npm run dev` (local)
- Logins: `saul@otr.do` (coach), `rosa.fermin@otr.do` (familia), `admin@otr.do` (admin)
- Viewport desktop 1280 y móvil 390; idioma ES y EN.

## Estado

**Completo — coach + familia + admin (2026-08-08)**

Notas de método para familia y admin (barrido del 8 ago, `PORT=3031`): el navegador del MCP
estaba compartido con otros agentes (pestañas saltando entre los puertos 3031/3032/3044), así
que ese tramo se corrió con un Chromium **aislado** lanzado desde Node con el Playwright ya
presente en disco — sin instalar nada. Cada pantalla se verificó a 1280 y 390, en ES y EN, con
click real en el DOM, midiendo `scrollWidth` vs `clientWidth`, desbordes por `getBoundingClientRect`,
texto cortado por `scrollWidth > clientWidth` y color fuera de paleta por `getComputedStyle`.

Sin ver, y por qué: no se pulsaron **Aprobar/Rechazar** de la aprobación pendiente, ni
Suspender/Borrar datos/Marcar revisado/Descartar, ni el alta de clase — mutan la `dev.db`
compartida con otros agentes. El **detalle de listing** (`scr-listing.ts`) no se pudo barrer:
el marketplace de clases tiene 0 registros en el seed. Los fallos de red hacia YouTube en la
ficha de coach son del entorno sin salida a internet, no del producto.

## Hallazgos

| rol | pantalla | estado | qué exactamente | archivo:línea | gravedad |
| --- | --- | --- | --- | --- | --- |
| coach | teacher (`#teacher`) | OK | Render limpio a 1280, 0 errores de consola. Pestañas Grupo/Contenido conmutan (Grupo: KPIs 90%/50%/66%/2 + tabla de 8 alumnos; Contenido: 5 cursos, 5 módulos, 16 lecciones, 3 exámenes). Sin scroll horizontal (scrollWidth=clientWidth=1280). | — | — |
| coach | teacher (`#teacher`) | defecto | Botones de acción por alumno (`.ev-actions .btn-outline`) son solo-icono con `title="Enviar mensaje"` y **sin `aria-label`**: lector de pantalla los anuncia vacíos. | app/lib/scr-teacher.ts | baja |
| coach | participants | OK | Render limpio, 0 errores. Filtros Todos·9 / Estudiantes·8 / Coaches·1 / En riesgo·2 y tabla con XP y último acceso. Sin scroll horizontal a 1280. | — | — |
| coach | participants → modal **Adjudicar** | OK | Abre `.modal.modal--v2` "Adjudicar ronda · Isabella Guzmán" con Resultado (2 opc.), Formato (4), Oponente (texto), Compañero (8 opc.), rúbrica de 5 sliders numéricos (valor 7) y Cancelar / Adjudicar y publicar. | — | — |
| coach | participants → modal Adjudicar | defecto | El modal mide **860 px de alto**; con 900 px de viewport queda a 20 px del borde y en portátiles de 800 px de alto los botones Cancelar/Adjudicar quedan fuera de pantalla (el `.modal` no tiene scroll propio). | app/lib/scr-teacher.ts (modal Adjudicar) | media |
| coach | coachwork | OK | Render limpio, 0 errores. Las 3 pestañas conmutan: Agenda (2 próximas sesiones + historial), Ingresos ($90 escrow / $45 liberado / $36.90 payout / take rate 18%), Disponibilidad (tarifa $45/h + 6 franjas). Sin scroll horizontal ni texto cortado a 1280. | — | — |
| coach | manage (Mis cursos) | OK | Render limpio, 0 errores. 5 cursos con badge PUBLICADO y trío Construir/Configuración/Eliminar. "Configuración" abre el modal "Editar curso" (nombre, formato, modalidad, cupo, video, layout, estado) y cierra bien. Sin scroll horizontal ni texto cortado a 1280. | — | — |
| coach | my-listings (Mis clases) | OK | Render limpio, 0 errores. Empty state correcto ("Aún no has publicado clases") y el botón "Publicar clase" abre el modal con materia (9 opciones), título, descripción, tarifa y modalidad; cierra bien. Sin scroll horizontal a 1280. | — | — |
| coach | my-listings vs coachwork | defecto | **Moneda inconsistente**: el modal de publicar clase pide "Tarifa por hora (**RD$**) *" mientras coachwork/Disponibilidad muestra "Tarifa por hora: **$45**/hora" y los ingresos van en `$` ($90 escrow, $36.90 payout). Mismo dato, dos monedas. | app/lib/scr-my-listings.ts vs app/lib/scr-coachwork.ts | media |
| coach | messages | OK (parcial) | Render limpio, 0 errores. Cambiar de conversación funciona (`.convo.active` se mueve) y el composer existe. Sin scroll horizontal a 1280. | — | — |
| coach | messages | defecto | La conversación abierta se etiqueta con **el propio coach**: cabecera y lista dicen "SM · Coach Saúl Méndez" aunque el hilo es con Analía ("¡Hola Analía! Vi tu diagnóstico…"). Debería mostrar la contraparte, como sí hace el hilo de Diego Fermín. | app/lib/scr-community.ts (bloque messages) | media |
| coach | messages | defecto | El hilo de **Diego Fermín** abre **vacío** (0 burbujas, solo el separador "Hoy") pese a que la lista previsualiza "Gracias coach · hace 2h". Preview y detalle no coinciden. | app/lib/scr-community.ts / app/lib/queries.ts | media |
| todos | **global (router)** | defecto | La navegación NO sincroniza la URL: tras pulsar "Participantes" `window.__route === 'participants'` pero `location.hash` sigue en `#teacher`. `renderApp()` se llama tras `e.preventDefault()` sin tocar el hash → sin deep-link, sin botón Atrás, y F5 devuelve a la pantalla anterior. | app/components/Aula.tsx:1114 | media |
| todos | **global (router, carga)** | defecto | Complemento del anterior: el hash tampoco se LEE al arrancar. `startRoute` sale solo del rol y `location.hash` no se consulta nunca (ni hay listener de `hashchange`), así que abrir `/aula#explore` o `/aula#admin-users` pinta siempre el home del rol. Los ítems del nav sí llevan `href="#ruta"`, o sea que "copiar enlace" produce URLs que no funcionan. | app/components/Aula.tsx:1187 | media |
| familia | parent (`#parent`) | OK | Render limpio a 1280 **y a 390**, 0 errores de consola, `scrollWidth == clientWidth` en ambos, sin texto cortado ni color fuera de paleta. KPIs 1 hijo / 0 próximas sesiones / 1 aprobación pendiente / $45 gasto del mes; ficha de Diego Fermín con badge "Menor — protegido", bloque de seguridad y consentimiento, umbral de aprobación y vinculación de otro estudiante. | — | — |
| familia | parent → modal Reporte mensual | OK | "Ver reporte de agosto" abre "Reporte mensual de Diego Fermín · agosto 2026" con datos reales (Nivel OTR Initiate, XP 820, inversión $45, asistencia 0 de 0) y botones Imprimir reporte / Cerrar. Alto 623 px, entra en viewport. | — | — |
| familia | explore / Coaches | OK | Render limpio 1280 y 390, 0 errores, sin scroll horizontal ni texto cortado. 2 coaches (Saúl Méndez, Carla Jiménez). Los filtros de materia **filtran de verdad** con click real: Oratoria→1, Policy→1, Public Forum→1, Todos→2; el buscador con "Carla"→1. Tarjeta con `role="button"`, `tabindex=0` y `aria-label="Ver perfil de Saúl Méndez"`. | — | — |
| familia | explore → ficha de coach | OK (parcial) | La tarjeta abre la ficha de Saúl Méndez con bio, credenciales, especialidades, 6 reseñas, packs (Single $45 / 5-pack $200 / 10-pack $380) y calendario de 10 días con franjas horarias. Sin scroll horizontal ni desbordes a 1280 y 390. | — | — |
| familia | explore → ficha de coach | defecto | **Etiqueta duplicada y en español dentro de la UI en inglés**: se lee "Responde en Responde en ~2 h" en ES y "Responds in **Responde en ~2 h**" en EN. La causa es doble: el seed guarda la frase completa en `responseTime` y la vista le antepone la etiqueta `t("mkt.respondsIn")`. | app/lib/scr-marketplace.ts:525 (valor en prisma/seed.ts:1133) | media |
| familia | explore → ficha de coach | defecto | La ficha **no tiene ningún `h1`/`h2`/`h3`**: el nombre del coach se pinta como `<b class="brand-font">` y los únicos encabezados son 6 `h4` (Sobre Saúl, Credenciales, Especialidades, Reseñas, Reserva tu sesión, Política de cancelación). Un lector de pantalla no encuentra el título de la pantalla ni puede saltar por jerarquía. | app/lib/scr-marketplace.ts:515 | baja |
| familia | listings (Buscar clases) | OK (parcial) | Render limpio 1280 y 390, 0 errores, sin scroll horizontal. 10 chips de materia (Todas·Debate·Oratoria·Inglés·Matemáticas·Ciencias·Programación·IA·Música·Otros) y buscador por materia. **El marketplace de clases está vacío en el seed** (0 listings), así que el detalle de un listing (`scr-listing.ts`) no se pudo barrer con datos reales. | — | — |
| familia | messages | defecto | Rosa no tiene conversaciones y **no hay ninguna forma de iniciar una**: en el estado vacío el único control de la pantalla es el buscador "Buscar…" (0 botones). El propio portal empuja a "Abrir mensajes" bajo el gancho "Habla con tus coaches", y el destino es un callejón sin salida. El composer solo existe dentro de un hilo ya abierto (línea 140). | app/lib/scr-community.ts:144 | media |
| familia | membership | OK | Render limpio 1280 y 390, 0 errores, sin desbordes. "Tu plan: OTR Free" con plan vigente marcado, comparativa Free vs Pro (US$9/mes · US$79/año) y CTA "Pasar a Pro" / "Próximamente". | — | — |
| familia | membership vs resto | defecto | **Tercera notación de moneda** en el producto: membresía usa `US$0` / `US$9` / `US$79`, el portal de familia y la ficha de coach usan `$45`, y el modal de publicar clase del coach usa `RD$`. Tres monedas para el mismo producto en la misma sesión. | app/lib/scr-lifetime.ts:523 y app/lib/queries.ts:1749 (vs scr-my-listings.ts) | media |
| familia | settings → cambio de idioma | OK | Click real en "EN" retraduce toda la interfaz al instante (cookie `otr_lang=en`, `<html lang="en">`, "Family Portal · Proof and peace of mind"), sin errores ni recarga. El botón "ES" lo revierte. Portal en EN limpio a 1280 y 390. | — | — |
| familia | parent (EN) | defecto | **Fechas en español dentro de la UI en inglés**: la aprobación pendiente se lee "Diego Fermín wants to book with Saúl Méndez **lun 10 ago** · 5:00 PM · $45". `dateLabel()` compone el día y el mes con tablas fijas en español (`DIAS`/`MESES`) sin rama de idioma; alimenta también las franjas de disponibilidad del coach. | app/lib/consultations.ts:60-67 | media |
| familia | profile | defecto | El perfil de una **madre** muestra la gamificación de alumno: rango "OTR Initiate", "Nivel 1", "1000 XP para el siguiente nivel", "0 XP · 0 días" de racha, "Insignias destacadas" y "Mis programas → Explorar programas". `S.profile` solo distingue dos caras (`isTeacher = teacher\|admin`), así que el rol `parent` cae en `renderStudentSelf()`. | app/lib/scr-profile.ts:187 | media |
| familia | settings | OK (parcial) | Render limpio a 1280, 0 errores. Cuenta (chip FAMILIA), selector de idioma ES/EN, 4 toggles de notificaciones, membresía, cambio de contraseña y cierre de sesión. | — | — |
| familia | settings @390 | defecto | En la tarjeta de cuenta el correo **se sale de su caja y queda tapado por el botón "Editar perfil"**: `rosa.fermin@otr.do` mide 118 px en un hueco de 99 px y solapa 5 px con el botón — en pantalla se lee "rosa.fermin@otr.d". Al `div` del email le falta `overflow:hidden; text-overflow:ellipsis; white-space:nowrap`. | app/lib/scr-settings.ts:168 | baja |
| admin | admin (Consola de moderación) | OK | Render limpio 1280 y 390, 0 errores, sin scroll horizontal ni texto cortado ni color fuera de paleta. KPIs "Reportes abiertos 1 / En la cola 1". Las **3 pestañas conmutan** con click real: Reportes (1 reporte de mensaje `cv-2` "pedía contacto fuera de la plataforma", reportado por Diego Fermín · 8 ago 2026, con Marcar revisado / Descartar), Auditoría y Clases (ambas con su empty state). | — | — |
| admin | admin (tabs) | defecto | Las pestañas son `<button class="tab active">` **sin semántica de pestaña**: no hay `role="tablist"`/`role="tab"`, ni `aria-selected`, ni `aria-controls`. Un lector de pantalla oye tres botones sueltos y nunca sabe cuál está activo ni qué panel gobiernan; el estado vive solo en el color. | app/lib/scr-admin.ts | baja |
| admin | admin-users | OK | Render limpio 1280 y 390, 0 errores, sin scroll horizontal. KPIs 12 usuarios / 2 coaches / 1 admin / 0 suspendidos. Los **filtros filtran de verdad** con click real: Coaches→2 (Carla Jiménez, Saúl Méndez), Familias→1 (Rosa Fermín), Admins→1 (Equipo OTR), Todos→12; el filtro activo se marca en negro (`btn-primary`). Cada fila trae selector de rol, Suspender y Borrar datos, más "Exportar CSV". | — | — |
| admin | admin-users | defecto | Las acciones **destructivas** de cada fila se llaman igual en las 12 filas: el nombre accesible de los botones es literalmente "Suspender" y "Borrar datos", sin `aria-label` ni `title` que nombre a la persona. Quien navegue por botones oye "Suspender" doce veces sin saber a qué cuenta aplica — y son acciones de suspensión y borrado de datos. | app/lib/scr-admin-users.ts | media |
| admin | admin-metrics | OK | Render limpio a 1280, 0 errores, sin scroll horizontal ni texto cortado ni color fuera de paleta. Datos reales: 4 alumnos activos (7 d), 12 usuarios, 4 reservas, embudo 8→8→8→5→3, registros por semana, usuarios por rol, membresías (11 Gratis / 1 Pro), reservas por estado, 5 cursos, 16 inscripciones, 3 torneos. **Las dos exportaciones funcionan**: `/api/admin/export?entity=enrollments` → 200 `text/csv` con 18 filas y `?entity=bookings` → 200 con 6 filas, cabeceras correctas y BOM. | — | — |
| admin | admin-metrics | defecto | El KPI **"GMV (escrow simulado)" muestra $0 aunque hay $130 en escrow** (4500 CONFIRMED + 4500 y 4000 COMPLETED). El agregado suma `Booking.priceCents`, pero las reservas del seed solo crean el `Escrow` con `amountCents` y dejan `priceCents` en su default 0. El CSV de la MISMA pantalla sí imprime los montos (usa `escrow.amountCents ?? priceCents`), así que el panel se contradice con su propia exportación. El flujo real de reserva sí fija `priceCents` (app/api/bookings/route.ts:209), o sea que es un hueco del **seed**, no del código de producción. | prisma/seed.ts:1216-1233 (agregado en app/api/admin/metrics/route.ts:85) | media |
| admin | admin-whatsapp | OK | Render limpio 1280 y 390, 0 errores, sin desbordes. Empty state correcto en dos paneles ("Sin conversaciones todavía" + "Selecciona una conversación") — coherente con un entorno dev sin credenciales de WhatsApp Business. El botón "Actualizar" responde sin errores de consola. | — | — |
| admin | admin-metrics @390 | defecto | **La exportación se sale de la pantalla**: los dos botones viven en un `.row` con `justify-content:flex-end` y `flex-wrap:nowrap`; suman 465 px en 360 disponibles, así que el primero se desborda hacia la IZQUIERDA (left = **-90 px**) y en pantalla se lee "**r inscripciones (CSV)**", sin icono y con 90 px fuera del viewport. No hay scroll horizontal que lo rescate (el desbordamiento es negativo). | app/lib/scr-admin-metrics.ts (fila de exportaciones, `justify-content:flex-end;gap:8px` sin `wrap`) | media |
| admin | 4 consolas en EN | OK (parcial) | Con `otr_lang=en` las cuatro pantallas traducen bien y sin errores: "Moderation console" (Reports·Audit log·Classes), "User management" (All·Students·Coaches·Families·Admins, Export CSV), "Business metrics" (GMV (simulated escrow)) y "WhatsApp" (Team inbox). Los defectos de layout y paleta se reproducen igual en EN. | — | — |
| admin | admin (EN) | defecto | **Fecha en español dentro de la UI en inglés**: el reporte se lee "Reported by Diego Fermín · **8 ago 2026**". La consola formatea con el locale fijo `"es"` en vez del idioma activo. | app/lib/scr-admin.ts:54 y :75 | baja |
| admin | manage (Cursos) | defecto | El ítem "Cursos" del nav de admin lleva a **"Mis cursos · Sin cursos todavía"** con "Nuevo curso" como único control, mientras la propia pantalla de Métricas del admin reporta **5 cursos publicados**. `S.manage` lee `DB.teacherCourses` (los cursos DE los que el usuario es dueño) y no tiene rama de admin, así que el admin no ve ni puede reasignar ninguno de los 5 — justo lo que shell.ts:121-123 dice que esta pantalla es para él ("el admin crea cursos a nombre de un coach y reasigna dueños"). El eyebrow además dice "Profesor". | app/lib/scr-extra.ts:251 | media |
| admin | profile | defecto | El perfil del admin se pinta con la **cara de coach**: chip de rol "Coach", "Perfil de marketplace", "0.0 Rating · 0 Reseñas" y "Cómo trabajo — Aún no has descrito tu metodología". Es el otro lado del binario de `S.profile` (`isTeacher = teacher \|\| admin`): al admin se le atribuye un perfil vendible de marketplace que no le corresponde. | app/lib/scr-profile.ts:187 | baja |
| admin | debate / events | OK | Ambas rutas del nav de admin renderizan limpias a 1280 y 390, 0 errores. En Debate Hub la tira de pestañas que se sale a 390 vive en un contenedor con `overflow-x:auto` (scroll intencional, no corte); en Eventos se ven los 3 torneos con Inscribirme/Editar/Borrar (borrado exclusivo de admin) y nada queda cortado en pantalla. | — | — |
| admin | admin-users | defecto | **Resto de paleta vieja**: el chip del rol "Profesor / Coach" usa `.chip--info` con `background:#E7EBEE; color:#3F5566` — un azul-pizarra frío, hardcodeado en hex y ajeno a la paleta. `tokens.css` declara "Paleta ESTRICTA: Negro #171717 · Naranja #F25623 como ÚNICO acento", y el guardián `tests/brand-palette.test.ts` no lo está cazando. | app/styles/screens.css:62 (uso en app/lib/scr-admin-users.ts:49) | media |
| familia | listings (Buscar clases) | defecto | Con el filtro **"Todas"** activo el empty state dice "Aún no hay clases **en esta materia**" — no hay materia seleccionada, el marketplace entero está vacío. El copy culpa a un filtro inexistente en vez de decir que aún no hay clases publicadas ("Prueba otra categoría" no lleva a ninguna parte: todas están vacías). | app/lib/scr-listings.ts:93 (`lst.emptyTitle`, app/lib/i18n-keys/lst.ts:28) | baja |

## Cierre de campaña (2026-08-08)

**Coach (6):** S1 aria-labels nominales, S3 moneda unificada a `$` (money() 21 usos vs 4 labels), S4 contraparte real con `conversationLabel()` puro, S5 hilo de Diego sembrado con el intercambio real y preview derivado del último mensaje — **CORREGIDOS** (F4). S2 (modal 860px) **NO ERA DEFECTO**: el techo `max-height:100vh-40` funciona; a 1280×800 el cuerpo scrollea y los botones quedan visibles (medido). El doble-escape de Mensajes que destapó la revisión también quedó al contrato (una capa, sonda XSS en vivo con 0 inyecciones).

**Familia + admin (16):** los 12 media/baja de E4 (ficha sin duplicado y con h1, perfiles por rol, aria nominales en Suspender/Borrar, GMV $130 cuadrando con CSV, export apilado a 390, tabs semánticos, **Cursos de admin con los 5 cursos y reasignación de dueño real vía el PATCH existente**, chip a tinte de paleta con guardián endurecido, email elidido, copy de listings) + los 4 de E5 (mensajes de familia con salida "Buscar coaches", membresía y torneos en `$`, fechas de admin y del portal familiar con el idioma activo) — **CORREGIDOS**. El doble-escape de bio/reseñas del perfil (`St. Michael's`) cerró en la ola final.

Quedan post-despliegue (sin riesgo, anotados en el informe): guard del select de reasignación cuando el dueño es ADMIN legacy, "Dueño" como rótulo (calco), panel de notificaciones sin Escape.
