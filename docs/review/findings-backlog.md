# OTR Aula — Findings Backlog (128, adversarially triaged)

Source: multi-agent workflow wf_b5e376ab-aa4 (52 agents). 26 high/critical confirmed, 10 refuted (excluded below).

## Refuted by adversarial verification (NOT fixing)
- [LRN-02] quiz: The fallback chain is cited accurately (scr-learn.ts currentQuiz() lines 40-48 do fall back to the first quiz in any enrolled course, then to any quizByLesson entry), but the harmful scenario the find
- [DEB-02] debateHub: Cited locations are accurate (TABS at line 27 with the six tabs as listed; renderTab at line 447; viewOverview at line 161 with recentCard/nextEventCard), but the core claim — "the same data re-presen
- [DEB-03] debateHub: The finding inverts the source of truth. It treats the stale header comment {name,wins,losses,draws} (scr-debate.ts lines 6-11) as the contract and the render fallbacks as a speculative guess. But the
- [LIFE-02] membership: Every defect this finding alleges is already handled by a centralized, intentional a11y layer the reviewer missed by reading only scr-lifetime.ts/scr-parent.ts and not the SPA engine. In app/component
- [COACH-03] manage: REFUTED. The finding's core technical premise is false because it ignores the data layer. teacherCourses[].name is ALREADY HTML-escaped server-side in queries.ts:1553 (`name: esc(c.name)`), and esc() 
- [PARENT-01] parentPortal: The finding's factual substrate is accurate but it is a subjective IA preference dressed as a high-severity defect, not a real defect. Verified in app/lib/scr-parent.ts: three per-child lists do exist
- [PARENT-03] parentPortal: The finding's core claims are refuted by current source. There is a centralized [A11Y-03] handler in app/components/Aula.tsx (L967-1015): a MutationObserver on document.body that enhances every .modal
- [IA-02] All roles (topbar search) -> search results (scr-extra.ts): The finding's file/line citations are accurate (shell.ts:173 mounts the searchbox with no role guard; Aula.tsx:953-956 routes all roles to 'search'; scr-extra.ts:296-297 queries DB.catalog + DB.studen
- [DS-01] Global (catalog, course, marketplace, dashboard): The underlying defect is technically real, but DS-01 is a re-filing of already-known bug #1 ("systemic '&amp;' double-escape"), which the prompt says must NOT be re-reported as new — and the finding i
- [SIMP-01] Shell nav (student) + progress/badges/lifetime/profile: The finding's structural facts check out but its core overlap claim is materially wrong, and what remains is a subjective IA preference, not a defect. Verified in source: the student 'group.progress' 

## CRITICAL (3)
- [LRN-01] **critical** · a11y · _quiz_
  - problem: The quiz answer options (.q-opt) and question-jump dots (.q-dot) are plain <div>s with click listeners attached in mount(); they have no role, no tabindex, and are never focusable. A keyboard or screen-reader user literally cannot select an answer or navigate questions — the single most important interaction in the Learn flow is mouse-only. The global [A11Y-01] Enter/Space handler in Aula.tsx (line 960) only fires for elements matching [role=button][tabindex], which these never get (unlike the .mitem and <tr> rows in scr-core.ts which DO get role/tabindex).
  - fix: In S.quiz.mount() paint(), render each .q-opt with role="radio" tabindex="0" inside a role="radiogroup" (aria-labelledby the question), and each .q-dot with role="tab"/button tabindex="0"; add an aria-checked/aria-current state. Either reuse the [A11Y-01] handler (give them role=button tabindex=0) or add keydown(Enter/Space + Arrow keys) handling in mount(). Add :focus-visible is already global so focus ring comes for free once they are focusable.
  - file: `app/lib/scr-learn.ts → S.quiz.mount() paint() (.q-opt / .q-dot rendering, ~lines 448-453); contract in app/components/Aula.tsx line 960` · effort M · conf 0.95
- [MKT-01] **critical** · a11y · _marketplace/explore (grid)_
  - problem: The coach cards are the PRIMARY interaction on the grid, but each is a plain `<div class="tile click" data-coach="...">` (coachCard, scr-marketplace.ts:189). They have no role, no tabindex, and no keyboard handler. The only global keyboard activator in Aula.tsx (line 960) fires solely for `[role="button"][tabindex]`, so the cards cannot receive focus or be opened by Enter/Space. A keyboard or screen-reader user cannot open any coach profile — the entire marketplace is unusable without a mouse.
  - fix: In coachCard (scr-marketplace.ts:189) add `role="button" tabindex="0" aria-label="Ver perfil de ${esc(c.name)}"` to the `<div data-coach>`. This piggybacks on the existing Aula.tsx Enter/Space handler at line 960 (which calls .click()), so no new JS is needed. Apply the same to the package/day/slot chips if they are `<button>` already they are fine; confirm pkg buttons are `<button>` (they are).
  - file: `app/lib/scr-marketplace.ts → coachCard (line ~189, the `<div ... data-coach>` element)` · effort S · conf 0.97
- [A11Y-01] **critical** · a11y · _Marketplace (explore) + Debate Hub_
  - problem: The primary cards on two flagship screens are keyboard-inoperable. Marketplace coach cards render as <div class="tile click" data-coach="..."> (scr-marketplace.ts:189) and Debate Hub cards as <div class="tile click" data-debate="..."> (scr-debate.ts:225). Each gets a mouse-only click listener (scr-marketplace.ts:512 opens the coach profile; scr-debate.ts:490 opens the debate detail) but NO role="button", NO tabindex, NO keyboard handler. The global [A11Y-01] activator in Aula.tsx:960 only fires for elements matching '[role="button"][tabindex]', so these cards can neither be focused via Tab nor activated via Enter/Space. A keyboard or screen-reader user literally cannot open a coach profile or a debate.
  - fix: Add role="button" tabindex="0" to the coach card div (scr-marketplace.ts:189) and the debate card div (scr-debate.ts:225); also add an aria-label (e.g. `aria-label="Ver perfil de ${esc(c.name)}"` / `aria-label="Abrir debate ${esc(h.title)}"`). With role=button+tabindex present, the existing Aula.tsx:960 handler already fires .click() on Enter/Space — no JS change needed. Apply the same pattern to any other .tile click / clickable div using data-* delegation.
  - file: `app/lib/scr-marketplace.ts (coachCard ~L189), app/lib/scr-debate.ts (~L225)` · effort S · conf 0.95

## HIGH (23)
- [CORE-01] **high** · fix-bug · _catalog_
  - problem: Course/coach names containing '&' render the literal entity '&amp;' on the catalog cards (and elsewhere). queries.ts builds DB.catalog with already-escaped values (queries.ts:1430 `name: esc(pickLang(...))`, `coach: esc(...)`, `format: esc(...)`, `modality: esc(...)`), but S.catalog re-escapes them.
  - fix: In app/lib/scr-extra.ts S.catalog.card, drop the redundant esc(): use ${c.name}, ${c.coach}, ${c.code} (code is not esc'd in queries so keep esc(c.code)). Verify against queries.ts:1430 which fields are pre-escaped and only esc the raw ones. Do the same audit, not blanket-strip.
  - file: `app/lib/scr-extra.ts (S.catalog.render → card: lines 185-189)` · effort S · conf 0.95
- [CORE-02] **high** · fix-bug · _search_
  - problem: The 'Personas' section of search is permanently empty for the student role — the only role that can reach search. S.search reads DB.students (scr-extra.ts:297), but queries.ts only populates base.students inside the isTeacher block (queries.ts:1514-1518); for a STUDENT it is undefined, so (DB.students||[]) yields [] every time. Result count and the people group are silently always courses-only.
  - fix: Either (a) expose a role-safe, privacy-scoped people index to students in queries.ts (e.g. cohort/leaderboard-derived rows with name+level only, same opt-in rules as leaderboard) and point S.search at it; or (b) if people-search is out of scope for students per PRD, remove the 'Personas' section from S.search and reword the page-sub to 'cursos' so the count and copy are honest. Prefer (b) if no privacy-cleared people source exists.
  - file: `app/lib/scr-extra.ts (S.search.render: lines 297, 307) + app/lib/queries.ts (base.students at 1514)` · effort M · conf 0.9
- [LRN-03] **high** · content · _assignment_
  - problem: The 'Calificación' card hardcodes an identical rubric for EVERY 100-point assignment — Estructura (CWI) 30 / Claridad y voz 25 / Evidencia 25 / Tiempo y cierre 20 (lines 196-199). This is presented as if it were this specific activity's grading breakdown, but it is a static debate-speech template shown regardless of what the assignment actually is (e.g. a written essay or a research upload). For non-100-point assignments it correctly falls back to a generic message — proving the 100-point branch is fabricated specificity.
  - fix: Render the rubric only from real data (L.rubric / L.rubricItems). If absent, use the same honest generic message already used for non-100-point assignments ('Tu coach la revisará y te dará una nota con feedback'). Remove the hardcoded array at lines 197-198.
  - file: `app/lib/scr-learn.ts → S.assignment.render(), rubric block lines 196-199` · effort S · conf 0.8
- [DEB-01] **high** · a11y · _debateHub_
  - problem: The debate history cards (viewHistory) and tournament cards (viewTournaments) are clickable <div class="tile click"> elements carrying data-debate / data-tournament-card, but they have no role="button", no tabindex, and no aria. The screen's mount() wires a plain click listener (root.querySelectorAll('[data-debate]')), so a mouse opens the ballot modal but a keyboard user cannot focus or activate the card at all. The app's own [A11Y-01] handler in Aula.tsx (line 960) only fires .click() for elements matching '[role="button"][tabindex]' — these cards match neither, so they are completely keyboard-inoperable.
  - fix: In scr-debate.ts add role="button" tabindex="0" (and an aria-label like 'Ver ballot vs <opponent>') to the history tile (line 225, data-debate) and the tournament card (line 355). The existing Aula.tsx onKey bridge will then activate them on Enter/Space — or add an explicit keydown handler in mount() alongside the click listener. Tournament cards that are purely informational (no data-tournament action) should NOT get role=button; only the ones that open detail.
  - file: `app/lib/scr-debate.ts (viewHistory line 225 tile; viewTournaments line 355 tile) + verify against Aula.tsx onKey line 960` · effort S · conf 0.92
- [MKT-02] **high** · a11y · _both (grid + profile)_
  - problem: Interactive/important green text uses `--action` (#2CAA20), which is 3.05:1 on white and 2.83:1 on cream — below the 4.5:1 AA threshold for normal text. Affected: the "Ver perfil" link (class="sky" → .sky{color:var(--action)}, scr-marketplace.ts:213), the "Marketplace" eyebrow (.eyebrow{color:var(--action)}, scr-marketplace.ts:243), and every price figure (class="cc-pct" → color:var(--action-hover) #1E8C16 ≈ 3.9:1, screens.css:42). The design system already ships `--otr-green-text` (#176B11 = 6.67:1) created expressly for green text, so this is avoidable drift.
  - fix: Repoint the text-only green to the AA token: change `.sky{color:var(--action)}` (app.css:24) to `color:var(--otr-green-text)`, and `.course-card .cc-pct{...color:var(--action-hover)}` (screens.css:42) / the reused .cc-pct figures to `--otr-green-text`. The eyebrow (.eyebrow, app.css:118) should also move to --otr-green-text. Pure-green stays fine for fills/icons; only text must change.
  - file: `app/styles/app.css (.sky line 24, .eyebrow line 118) + app/styles/screens.css (.cc-pct line 42)` · effort S · conf 0.9
- [MKT-03] **high** · a11y · _both_
  - problem: Neither screen exposes a real heading structure. The grid title is `<div class="page-title">Coaches</div>` (renderGrid:244); on the profile the coach name is `<b class="brand-font">` (renderProfile:441) and every section label ("Sobre …", "Reseñas", "Reserva tu sesión", "Credenciales", "Especialidades") is a bold `<b>` (lines 462,469,370,464,465). A screen-reader user gets no heading landmarks to navigate by and no programmatic page title; the visual hierarchy is faked with font-weight only.
  - fix: Make the grid title an `<h1>` (or `<h1 class="page-title">`) in renderGrid (line 244) and the coach name an `<h1>` in renderProfile (line 441). Promote the card section labels ("Sobre …" 462, "Reseñas" 469, "Reserva tu sesión" 370) to `<h2>`/`<h3>` keeping the existing inline font-size. This is structural markup only — no visual change.
  - file: `app/lib/scr-marketplace.ts → renderGrid (line 244) + renderProfile (lines 441, 462, 469) + bookingCard (line 370)` · effort M · conf 0.85
- [LIFE-01] **high** · a11y · _lifetimeProfile_
  - problem: The entire Skill Graph attribution feature is keyboard-inoperable. Each skill row is a clickable <div data-lpskill> with style="cursor:pointer" but NO role, NO tabindex, and the mount() handler binds only a 'click' listener (scr-lifetime.ts line 174 + line 428). A keyboard or screen-reader user cannot expand 'Qué lo movió' (the per-skill event history), and the expand/collapse state change is never announced.
  - fix: In skillRows() add role="button" tabindex="0" aria-expanded="${open}" to the <div data-lpskill> wrapper (and an id on the expanded panel + aria-controls). In mount(), alongside the click listener, add el.addEventListener('keydown', e=>{ if(e.key==='Enter'||e.key===' '){ e.preventDefault(); el.click(); } }) — mirroring scr-teacher.ts:243.
  - file: `app/lib/scr-lifetime.ts — skillRows() (line ~174) and S.lifetimeProfile.mount() (line ~427)` · effort S · conf 0.95
- [COACH-01] **high** · a11y · _participants_
  - problem: The participant filter chips (Todos / Estudiantes / Coaches / En riesgo) are bare <span class="chip"> with a click-only handler — no role, no tabindex, no keydown. A keyboard or screen-reader user cannot operate the only mechanism for narrowing the roster. This is doubly jarring because the SAME cluster does it correctly: the teacher screen's 'En riesgo' chip (#tr-risk) has role="button" tabindex="0" plus an Enter/Space keydown handler.
  - fix: Mirror the teacher pattern: add role="button" tabindex="0" (and aria-pressed reflecting .active) to each [data-filter] chip in render(), and in mount() add a keydown listener on #pt-filters that triggers the same activeFilter switch on Enter/Space. Better: extract a shared makeChipKeyboardable(el) helper so teacher and participants share one implementation.
  - file: `app/lib/scr-teacher.ts (S.participants.render chips lines 626-629; S.participants.mount filter handler lines 675-682)` · effort S · conf 0.95
- [COACH-02] **high** · eliminate · _teacher_
  - problem: The roster's '7 días' sparkline column does not plot real data. Each row renders one of exactly three hardcoded bar arrays chosen by a single s.trend flag ([40,55,50,68,72,80,88] for up, a mirrored array for down, a flat array otherwise). Two students both flagged 'up' get a byte-identical chart. The column header promises a 7-day activity trend; it delivers a decorative glyph keyed off one boolean.
  - fix: Either (a) feed a real 7-point series from the data layer (e.g. s.trend7:[...]) and plot that, or (b) if no daily data exists, remove the sparkline column entirely and keep the textual Engagement pill + Últ. acceso, which already carry the signal. Given SIMPLIFICATION priority and that the data does not exist, prefer removal: drop the <th class="center">7 días</th> and the corresponding <td>spark(...)</td>, and delete the now-unused spark() helper (line 7).
  - file: `app/lib/scr-teacher.ts (header line 93 col '7 días'; cell line 101; helper line 7)` · effort S · conf 0.85
- [CW-01] **high** · fix-bug · _coachwork › Agenda (empty state)_
  - problem: The empty-state CTA reads "Ver mi perfil en el marketplace" but its handler is data-go="explore", which routes to the marketplace BROWSE list of all coaches (screens.ts: explore → screen 'marketplace', crumbs ['Marketplace','Coaches']), not the coach's own profile. The coach is promised a view of their own listing and instead lands on the generic shopping list. Note the very next empty state (viewAvailability) does this correctly with data-go="coach" (the own-profile editor).
  - fix: In scr-coachwork.ts viewAgenda() empty state, change data-go="explore" to data-go="coach" (matching viewAvailability's empty-state CTA). Optionally keep wording as "Editar mi perfil de coach" to match the 'coach' route's actual content.
  - file: `app/lib/scr-coachwork.ts › viewAgenda() empty-state button (line ~209)` · effort S · conf 0.92
- [CW-02] **high** · simplify · _coachwork › Agenda (booking rows)_
  - problem: The PENDING status is rendered as a full sentence inside a pill badge: <span class="badge warn">…Esperando consentimiento del padre</span>. .badge is height:22px, font-weight:700, border-radius:pill (app.css:197) — it is built for 1-2 word states (Confirmada, Completada). A ~30-char string in a fixed-height pill either overflows, wraps into a lumpy multi-line pill, or pushes the row layout, especially in the wrap row on mobile. It also out-weights the actual status it sits next to.
  - fix: In statusBadge() set the PENDING pill to a short label (e.g. "Pendiente") and move "Esperando consentimiento del padre" into the faint subtext line of bookingRow() (the line that already shows slot · pkg · amount), or render it as a quiet inline note under the row. Keep the pill to ≤2 words like every other state.
  - file: `app/lib/scr-coachwork.ts › statusBadge() (line ~158) + bookingRow() subtext` · effort S · conf 0.85
- [CW-03] **high** · a11y · _coachwork › Disponibilidad (add slot form)_
  - problem: The 'Añadir franja' control is three bare <select> (#cw-av-day, #cw-av-start, #cw-av-end) plus a faint 'a' separator and an 'Añadir' button, with NO <label>, aria-label, or programmatic association. A screen-reader user hears three unlabeled combo boxes; the only field hint ('Añadir franja') is a separate <p class="label"> not tied to any control. The day select also defaults to Monday and times to 4–6 PM silently.
  - fix: Add aria-label to each select in viewAvailability() ('Día de la semana', 'Hora de inicio', 'Hora de fin') or wrap each in a <label>. Make the 'a' separator aria-hidden. This is a localized, low-risk fix.
  - file: `app/lib/scr-coachwork.ts › viewAvailability() #cw-av-day/#cw-av-start/#cw-av-end (lines ~347-353)` · effort S · conf 0.9
- [PARENT-02] **high** · a11y · _parentPortal_
  - problem: Form controls are not programmatically labeled. (1) The per-child approval-threshold <select data-threshold-child> (L338) has NO <label> and NO aria-label; its only identification is the adjacent visible child-name <span>, which is not associated. A screen-reader user hears 'combo box, Aprobar cada reserva' with no idea which child it governs. (2) The link-email <input id=gp-email> uses <label class=label> with no for= and the input has no id-matched label or aria-label (L191-192). (3) The report child <select id=pr-child> has the same unassociated <label> (L226-227).
  - fix: In scr-parent.ts add aria-label to the threshold select e.g. aria-label="Umbral de aprobación para ${esc(k.name)}" (L338); add aria-label to the public-profile toggle similarly; and either add a for=/id pair or wrap the input in its <label> for #gp-email (linkForm L191-192) and #pr-child (reportCard L226-227).
  - file: `app/lib/scr-parent.ts (threshold select L338; linkForm L188-196; reportCard L224-230)` · effort S · conf 0.9
- [ADM-01] **high** · merge · _adminUsers_
  - problem: The role <select> offers FIVE options including BOTH 'Profesor / Coach' (TEACHER) and 'Coach' (COACH) as separate, distinct choices. The whole app treats them as the same thing: isCoachRole(r)=>r==='TEACHER'||r==='COACH', the coach KPI counts TEACHER+COACH together, the 'Coaches' filter chip maps to both, and the backend (route.ts L60) merges them. An admin is forced to choose between two options that mean the same role with no stated difference — a textbook ambiguous/duplicate choice.
  - fix: Remove the 'COACH' option from ROLE_OPTS so the picker only offers STUDENT / Familia / Profesor (Coach) / Administrador (4 options). Keep COACH in ROLE_LABEL only so existing COACH rows still render a badge correctly. Normalize on TEACHER as the single coach role the UI can assign. File: app/lib/scr-admin-users.ts symbol ROLE_OPTS.
  - file: `app/lib/scr-admin-users.ts (ROLE_OPTS, L26-32)` · effort S · conf 0.88
- [ADM-02] **high** · a11y · _adminUsers_
  - problem: The role filter chips are clickable <span class="chip" data-au-role> with no role="button", no tabindex, and no keydown handler — they are completely keyboard-inoperable. This is the ONLY chip-as-filter in the app that breaks the established pattern: marketplace uses <button class="chip"> (scr-marketplace.ts L250/392/397) and scr-teacher.ts uses <span class="chip" role="button" tabindex="0"> (L89). The same component drift hides the entire role-filter feature from keyboard and screen-reader users.
  - fix: Make the chips <button class="chip"> (matches marketplace) or add role="button" tabindex="0" + an Enter/Space keydown handler (matches scr-teacher). Prefer <button> for least code. File: app/lib/scr-admin-users.ts render() chips template (L133-134) + the [data-au-role] handler (L225-226).
  - file: `app/lib/scr-admin-users.ts (render() chips, L133-134; handler L225-226)` · effort S · conf 0.92
- [ADM-03] **high** · redesign · _adminConsole_
  - problem: A report card identifies its target only by a raw opaque ID rendered as a chip: <span class="chip soft">${r.targetId}</span> (reportCard L63). For targetType 'user'/'coach'/'message'/'booking' the moderator sees e.g. 'ckp9f2a0x0001' with no name, no link, no preview. To decide whether to suspend a user the admin must recognize a human, but the screen only offers a database key to recall.
  - fix: Replace the raw targetId chip with a human label: show targetName when present and demote the raw id to a faint monospace sub-line (or drop it). Add a 'Ver' link that navigates to the target (window.go) where a route exists (user profile / coach profile / booking). If the API does not yet return targetName, add it server-side; otherwise gate the suspend button until a name is shown. File: app/lib/scr-admin.ts symbol reportCard (L54-88).
  - file: `app/lib/scr-admin.ts (reportCard, L54-63)` · effort M · conf 0.8
- [PROF-01] **high** · a11y · _profile_
  - problem: In renderStudentSelf the 'Mis programas' rows are clickable <div class="card card-pad lift" style="cursor:pointer" onclick="go('course')"> with NO role, NO tabindex, and NO keyboard handler (scr-profile.ts line ~301). The codebase's [A11Y-01] keyboard-activation handler in Aula.tsx (~line 960) only fires for elements matching [role="button"][tabindex]. These rows match neither, so keyboard and screen-reader users literally cannot open their own enrolled courses from their profile — the single primary action of the student profile body.
  - fix: Add role="button" tabindex="0" and an aria-label (e.g. `aria-label="Abrir ${esc(c.name)}"`) to the clickable course <div> in renderStudentSelf (scr-profile.ts ~line 301), matching the [A11Y-01] pattern used for lesson rows elsewhere. The existing Aula.tsx onKey handler will then make Enter/Space work for free.
  - file: `app/lib/scr-profile.ts (renderStudentSelf, 'Mis programas' clickable course div ~line 301)` · effort S · conf 0.95
- [PROF-02] **high** · a11y · _profile_
  - problem: No screen in the cluster has a programmatic top-level heading. progress and badges use a non-semantic <div class="page-title"> (scr-profile.ts lines 77, 141). profile/coach skip straight to <h2> for the person's name (lines 206, 279, 359) with no h1 above it. Screen readers get a heading tree that starts at h2 (or has no heading at all on progress/badges), and the document outline has no page title.
  - fix: Promote the visual page title to a real heading: make the .page-title element an <h1> in progress/badges, and on profile/coach wrap the page in an h1 (visually hidden or the existing title) so the name <h2> sits under a proper h1. Keep one h1 per screen.
  - file: `app/lib/scr-profile.ts (page-title divs lines 77 & 141; profile/coach <h2> name lines 206, 279, 359)` · effort S · conf 0.85
- [CERT-01] **high** · fix-bug · _certificate_
  - problem: The 'Imprimir' button calls window.print() but there is NO print stylesheet scoped to the diploma. The only @media print block in screens.css (lines 361-366) targets #pr-print (the admin report modal). Printing the certificate therefore renders the entire app chrome — dark sidebar, topbar, breadcrumbs, and the page-head action row containing the 'Imprimir'/'Volver' buttons themselves — around the diploma. A diploma is the one artifact users will actually print/PDF, so this is the highest-visibility failure on the screen.
  - fix: Add an @media print block in app/styles/screens.css that, when the certificate is on screen, hides the shell and the page-head action row and shows only .diploma-wrap. Concretely: give the diploma container a stable hook (e.g. id="cert-print" on .diploma-wrap in scr-certificate.ts) and add `@media print{ body:has(#cert-print) .app-sidebar, body:has(#cert-print) .topbar, body:has(#cert-print) .page-head{display:none!important} body:has(#cert-print) #cert-print{margin:0!important} }` mirroring the existing #pr-print pattern. Mark the page-head .row of buttons with data-noprint.
  - file: `app/styles/screens.css (@media print) + app/lib/scr-certificate.ts (S.certificate.render, .diploma-wrap container + print button row)` · effort S · conf 0.9
- [IA-01] **high** · fix-bug · _Coach Workspace empty-state (coachwork) -> coach profile_
  - problem: The coach onboarding empty-state button 'Activa tu perfil de coach' routes to data-go="coach", which is the read-only PUBLIC coach profile (S.coach / renderStudentSelf view). That route is registered with nav:'profile' and crumbs ['OTR','Coach'], so the coach lands on a non-editable page, the sidebar highlights 'Mi perfil' (implying it's their editable profile when it is not), and the breadcrumb reads a generic 'OTR / Coach' that doesn't match where they are. The coach's actual editable profile is r:'profile' (renderCoachSelf, per the explicit comment in shell.ts lines 61-62).
  - fix: In app/lib/scr-coachwork.ts line 304 change data-go="coach" to data-go="profile" so the coach lands on their editable profile (renderCoachSelf). Separately, in app/lib/screens.ts give the 'coach' route honest crumbs (e.g. ['Marketplace','Coach']) and consider nav:'explore' rather than nav:'profile' so viewing a public coach never highlights the viewer's own Perfil item.
  - file: `app/lib/scr-coachwork.ts (line 304, empty-state CTA); app/lib/screens.ts (route 'coach', line 57)` · effort S · conf 0.92
- [A11Y-02] **high** · a11y · _Course (Mi aprendizaje, module accordion) + Trayectoria (lifetime skill rows)_
  - problem: Two more primary interactions are keyboard-inoperable for the same reason. (1) The course module accordion header is <div class="module-head" data-acc> (scr-core.ts:415) with a mouse-only toggle (scr-core.ts:493) and no role/tabindex — modules cannot be expanded/collapsed by keyboard. (2) The trajectory skill rows are <div data-lpskill="..." style="cursor:pointer"> (scr-lifetime.ts:174) that expand a 'what moved this skill' panel on click, again with no role/tabindex/keyboard path.
  - fix: On scr-core.ts:415 make the accordion header keyboard-operable: add role="button" tabindex="0" plus aria-expanded reflecting the open state (toggle it in the data-acc handler at scr-core.ts:493) and aria-controls pointing at .module-items. On scr-lifetime.ts:174 add role="button" tabindex="0" aria-expanded="${open}" and an aria-label. Both then activate via the existing Aula.tsx:960 handler.
  - file: `app/lib/scr-core.ts (module-head ~L415, handler L493), app/lib/scr-lifetime.ts (data-lpskill ~L174)` · effort M · conf 0.92
- [A11Y-03] **high** · a11y · _All screens (systemic) — esp. Debate, Marketplace, Coach panel, Parent, Admin, Trayectoria_
  - problem: The page's main title is rendered as a non-semantic <div class="page-title"> in 42 places; only 2 screens use a real <h1>. Worse, scr-debate.ts, scr-marketplace.ts, scr-teacher.ts, scr-parent.ts, scr-admin.ts and scr-lifetime.ts contain NO <h1> at all and jump straight to <h3>/<h4>. Screen readers building a heading outline find either no top-level heading or an out-of-order one for the screen's own title.
  - fix: Change the page-title element from <div class="page-title"> to <h1 class="page-title"> (the CSS class already carries all visual styling, so appearance is unchanged) everywhere it represents the screen title; for the heading-less screens (debate/marketplace/teacher/parent/admin/lifetime) introduce an <h1 class="page-title"> as the screen title and demote the current top heading accordingly so levels don't skip. Best done via a shared pageHead() helper so future screens inherit one <h1>.
  - file: `app/lib/scr-core.ts (page-title ~L361,L513) + scr-debate.ts, scr-marketplace.ts, scr-teacher.ts, scr-parent.ts, scr-admin.ts, scr-lifetime.ts` · effort M · conf 0.88
- [COG-01] **high** · merge · _Coach · Seguimiento del grupo (teacher)_
  - problem: S.teacher.render() concatenates two complete pages on one route: a tracking dashboard (page-head 'Seguimiento del grupo' + 4 KPIs + 7-column roster + 'Requieren atención' rail + 'Pendientes de calificar' card) and then managePanel() which is itself a full page (its own page-head 'Estructura del curso' + 4 more KPIs + the Cursos→Módulos→Lecciones→Examen tree). The coach lands on ~8 KPI tiles and two distinct primary objectives (monitor students vs. author content) with no single obvious next action. This is the densest screen in the app and the only one with two page-heads.
  - fix: Split into two tabs on the teacher route ('Grupo' | 'Contenido') reusing the existing .tabs pattern (as debate/coachwork already do), OR move managePanel() to its own 'manage'/'course-builder' route (which already exists — there is a 'Editor completo' button to it) and replace the inline panel with a single compact link card. Either way render() should surface ONE primary action (Calificar entregas when pendingSubs>0, else first at-risk student). Drop the 4 'Estructura del curso' KPIs (Cursos/Módulos/Lecciones/Exámenes) from this screen — they are vanity counts, not decisions.
  - file: `app/lib/scr-teacher.ts (S.teacher.render + managePanel, lines ~56-200)` · effort M · conf 0.82

## MEDIUM (56)
- [CORE-03] **medium** · eliminate · _dashboard_
  - problem: The Achievements card's footer button is a verbatim duplicate of the hero's primary CTA — same icon, same label, same onclick (scr-core.ts:308 reuses na.onclick/na.cta from the hero at 96-114). The page now shows the identical primary action twice above the fold, and the hero CTA is the visually dominant navy/green button.
  - fix: In scr-core.ts achievements card, either remove the duplicate button entirely (the hero already carries that CTA) or repoint it to a genuinely different, badge-relevant action (e.g. onclick=go('badges') 'Ver insignias'). Removal is the simpler, higher-value fix.
  - file: `app/lib/scr-core.ts (S.dashboard → achievements, line 308)` · effort S · conf 0.85
- [CORE-04] **medium** · simplify · _dashboard_
  - problem: Above-the-fold the student dashboard stacks: hero + 4 KPI tiles + skill radar (6 bars) + 3 reco cards + (right rail) upcoming + debate rank + achievements + leaderboard strip. That is 8+ distinct card regions and ~25 discrete data points competing on first paint; the capture also flags the 4 KPI cards as very tall on mobile (heavy scroll for 4 numbers).
  - fix: (1) Collapse the 4 KPI tiles to a 2x2 compact grid on mobile (already a candidate in capture) — adjust .grid.g-4 responsive rule in app/styles/responsive.css. (2) Visually subordinate the reco + leaderboard blocks (smaller heading weight / muted) so hero + next-session read as primary. No data removal needed; this is hierarchy, not deletion.
  - file: `app/lib/scr-core.ts (S.dashboard layout, kpis at 135-141) + app/styles/responsive.css (.grid.g-4)` · effort M · conf 0.7
- [LRN-04] **medium** · a11y · _quiz, quizResults, assignment, player_
  - problem: Heading semantics are inconsistent across the cluster. S.lesson correctly uses <h1 class="page-title">. But S.quiz and S.assignment render their page title as a <div class="page-title"> (no heading element at all), and S.quizResults and S.player start at <h2> with no <h1> on the page. The quiz question itself (.q-text, lines 449) is a styled div, not a heading, so SR users get zero heading landmarks while taking an exam.
  - fix: Make the top page title an <h1> on quiz/assignment/quizResults/player (reuse .page-title styling on an <h1>). Render the quiz question prompt as an <h2>/<h3> instead of <div class="q-text">. Align with S.lesson which already does this.
  - file: `app/lib/scr-learn.ts → S.quiz.render() line 406 + paint() line 449, S.assignment.render() line 147, S.quizResults.render() line 537, S.player.render() line 606` · effort S · conf 0.85
- [LRN-06] **medium** · eliminate · _lesson_
  - problem: For a REAL lesson the in-lesson outline (aside.lesson-outline) collapses to a single dead anchor: <a href="#" onclick="return false" class="active">{title}</a> (line 604). It is a no-op link that duplicates the page title and provides no navigation — the 'En esta lección' rail only does real anchor-jumping for the legacy demo prose (#s1/#s2/#s3). So in production every real lesson shows a TOC rail containing one fake link to itself.
  - fix: For real lessons either (a) hide the outline rail entirely (let lesson-wrap go single-column) when there are no in-content headings, or (b) parse <h2>/<h3> from L.contentHtml at render and build real #anchor links. Do not render a self-link with onclick=return false. Fix in S.lesson.render() lines 601-607.
  - file: `app/lib/scr-core.ts → S.lesson.render() lesson-outline aside, lines 601-607` · effort M · conf 0.8
- [LRN-07] **medium** · merge · _player, lesson_
  - problem: Completion is handled two different ways across two screens for the same act. In S.player the 'Marcar como completada' button sits in the header row next to a 'Volver' button (lines 609-612), while in S.lesson the same control lives down in the outline aside (lines 609-613). Same data-action, two placements. Additionally the player has both a header 'Volver' (line 610) and a footer 'Volver al curso' when there is no next item (nextLabel, line 591) — two 'back to course' affordances on one short screen.
  - fix: Standardize: put 'mark complete' in the same location on both (recommend the primary action row near the bottom/next-button, since completion logically follows consuming the content). On player, drop the header 'Volver' and keep a single back path; let the footer hold {mark complete}{next/back to course}.
  - file: `app/lib/scr-learn.ts → S.player.render() lines 604-618; app/lib/scr-core.ts → S.lesson.render() lines 595-614` · effort M · conf 0.7
- [LRN-08] **medium** · automate · _quiz_
  - problem: During the quiz there is no persistent 'answered X of Y' summary or save state — progress is encoded only in small numbered dots, and all answers live in a mount() closure (answers object, line 427). Navigating away (or an accidental go()) discards every answer with no warning and no resume. The only completeness feedback is a toast at finalize time telling you how many are missing (line 466), after the user thinks they are done.
  - fix: (1) Add a small always-visible 'Respondidas N/total' counter near the dots or the finalize button. (2) Persist answers to sessionStorage keyed by quiz.id so a teardown/navigation can restore. (3) Optionally confirm before leaving with unsaved answers. Lands in S.quiz.render()/mount(), lines 412-419 (counter) and 421-489 (state).
  - file: `app/lib/scr-learn.ts → S.quiz.render() lines 412-419 and S.quiz.mount() lines 421-489` · effort M · conf 0.7
- [LRN-09] **medium** · a11y · _assignment_
  - problem: The voice-recorder controls (#rec-reset, #rec-toggle, #rec-play, lines 166-168) are icon-only buttons whose only accessible name is a title attribute. title is not reliably announced by screen readers and is invisible to touch users. The recorder waveform bars and timer also have no aria-live, so a SR user gets no feedback that recording started/stopped or how long it has run (status text in #rec-status is updated but the node is a plain div, not a live region).
  - fix: Add aria-label to #rec-reset/#rec-toggle/#rec-play (e.g. 'Grabar', 'Reproducir', 'Descartar') and update toggle's aria-label when it flips to pause. Add role="status" aria-live="polite" to #rec-status (line 164) so 'Grabando…'/'Grabado mm:ss — guardado' are announced.
  - file: `app/lib/scr-learn.ts → S.assignment.render() recorder controls lines 164-168` · effort S · conf 0.8
- [DEB-04] **medium** · merge · _debateHub_
  - problem: This file ships its own openModal() (line 621) and its own .seg segmented-button wiring (line 631) and its own modal foot markup, duplicating the modal machinery already centralized in Aula.tsx (multiple openModal-style helpers at lines 102, 151, 238, 276, 427, 553). It works because the global MutationObserver enhancer ([A11Y-03], Aula.tsx line 967) retro-fits aria-modal/focus-trap/Escape onto ANY .modal-scrim — but the screen-local modal is the only one that re-implements segmented buttons and the data-ok/data-x lifecycle by hand, creating drift risk (e.g. the local okBtn loading state uses textContent='Guardando…' while no shared spinner).
  - fix: Extract a single openModal/openFormModal helper (it already exists in Aula.tsx) and have scr-debate.ts call it instead of its private copy at line 621. At minimum, delete the redundant .seg wiring (line 631) since a shared helper should own it. The A11Y enhancer already covers aria, so this is purely de-duplication.
  - file: `app/lib/scr-debate.ts — openModal (line 621-658) duplicates Aula.tsx modal helpers` · effort M · conf 0.7
- [DEB-05] **medium** · fix-bug · _debateHub_
  - problem: Tournament registration optimistically mutates client state and never reconciles. In mount() (line 495), on a successful POST the code does (DB.tournaments||[]).forEach(t=>{ if(t.id===id) t.registered=true }) and repaints. There is no re-fetch, so the badge flips to 'Inscrito' based purely on a 200 — if the server later changes status (waitlist, capacity), the UI lies until a full reload. Worse, the record-debate flow's onDone callback (openRecordDebate line 615) just calls repaint() which re-runs getDebate() against the SAME in-memory DB.debate — a newly recorded debate will NOT appear in 'Mis debates' or move KPIs until the app refetches, yet the toast says 'Debate registrado' implying it landed.
  - fix: After a successful POST /api/debates (line 613) and POST /api/tournaments (line 502), call the global refresh() / re-hydrate getAppData before repaint(), instead of (or in addition to) the local mutation. Then 'Mis debates' and KPIs reflect the new round and the tournament badge reflects server truth.
  - file: `app/lib/scr-debate.ts — mount() tournament handler (line 502-505) and openRecordDebate onOk/onDone (line 613-616)` · effort M · conf 0.66
- [DEB-06] **medium** · a11y · _debateHub_
  - problem: Several decorative-but-informational badges sit below readable size/contrast. The recent-form pills in heroPanel render result deltas at 11px (line 143) and the 'Forma reciente' tiles encode WIN/LOSS only by a single colored letter (W/L/D) on color — color alone conveys outcome (WCAG 1.4.1). In the practice finder the proximity badge uses font-size:10.5px (line 308) and in Analytics the W/L/D legend badges are 10.5px (line 390). The diff badge toggles var(--ok) green vs --otr-sky which is ALSO green (tokens.css: --otr-sky aliases --otr-green) — so 'close match' (ok) and 'further match' (sky) are nearly the same hue, defeating the encoding.
  - fix: (1) Add a text/aria label to form pills (title is present but the visible letter is the only cue — keep the W/L/D letter AND keep a non-color cue). (2) Raise the 10.5px/11px badges to >=12px. (3) For the proximity badge (line 308) replace the ok/sky pair with two genuinely distinct tokens (e.g. ok green vs warn gold) since --otr-sky is green and visually identical to ok.
  - file: `app/lib/scr-debate.ts — heroPanel form pills (line 141-143), viewPractice finder badge (line 308), recordRow legend (line 390)` · effort S · conf 0.74
- [DEB-07] **medium** · content · _debateHub_
  - problem: The hero leads with 'Tu rating Glicko-2' and '±350 RD · provisional' (heroPanel line 120-125). For the Spanish-first student / non-technical / elderly persona, 'Glicko-2' and 'RD' (rating deviation) are unexplained jargon presented as the single largest, most prominent element on the flagship screen. The provisional alert helps, but the algorithm name and the RD figure are Optional/Noise to ~95% of users — they cannot act on them.
  - fix: Relabel the eyebrow to 'Tu rating' (drop 'Glicko-2' into a small info tooltip). Hide the raw '±RD' behind the existing title tooltip pattern or render it only when non-provisional. Keep the human-readable tier badge as the prominent secondary. This is a pure content cut, no layout change.
  - file: `app/lib/scr-debate.ts — heroPanel eyebrow (line 120) and RD line (line 125)` · effort S · conf 0.6
- [MKT-04] **medium** · fix-bug · _coach profile + booking_
  - problem: When a coach has no published availability, availRows() synthesizes a generic lun–sáb 9–18 window (line 124). But slotsFor() filters out anything inside the 12h LEAD_MS (line 142,150). On the current day or when the next eligible day's morning is within 12h, the user can land on a selected day whose slots are all filtered, showing "Sin horarios libres este día — prueba con otro" (line 398) while step 1 (package) and step 2 (day) look complete. The confirm button stays disabled with no explanation of WHY, and there is no guidance that this is a fabricated schedule the coach will re-confirm. First-timers can dead-end here believing the coach is unbookable.
  - fix: In bookingCard: when `generic` is true and `slots.length===0`, replace the terse empty-slot copy (line 398) with an actionable line ("Este coach confirmará el horario contigo — elige otro día o envíale un mensaje") and surface the existing "Enviar mensaje" CTA inline. Additionally, when the confirm button is disabled (ready===false), render a one-line hint under it stating the missing step ("Elige paquete, día y hora para continuar"). 
  - file: `app/lib/scr-marketplace.ts → bookingCard (slots empty branch line ~398; disabled CTA line ~411) + slotsFor/availRows interaction (lines 122-156)` · effort M · conf 0.78
- [MKT-05] **medium** · simplify · _marketplace/explore (grid)_
  - problem: The filter zone stacks up to 8 specialty chips (renderGrid:239 slice(0,8)) PLUS three dropdowns (idioma / precio / orden, lines 253-269) PLUS the conditional clear button — on a marketplace that, per the data model and demo, holds a small number of coaches. That is a lot of decision surface (Hick's) for a short list, and the spec chips duplicate filtering intent with no search box, which would be the higher-value control.
  - fix: Simplify: (1) collapse the three selects into a single ordering control plus an optional filter popover, OR (2) at minimum hide the spec-chip row behind a 'Filtros' toggle until the catalog exceeds ~9 coaches (data-driven: `all.length`). Add a single text search input (name/specialty) as the primary control — it absorbs the spec chips (Tesler's). Keep [COG-07] clear-filters logic.
  - file: `app/lib/scr-marketplace.ts → renderGrid (specs chips line 249-251; selects line 252-269)` · effort M · conf 0.6
- [MKT-06] **medium** · fix-bug · _marketplace/explore (grid)_
  - problem: The systemic `&amp;` double-escape (known bug #1) has a live instance in this cluster: coach specialty "Lincoln-Douglas & Policy" renders as "Lincoln-Douglas &amp;amp; Policy" in the card specialty badges (coachCard:206) and the profile specialties (renderProfile:465). The values arrive already HTML-escaped from the data layer and esc() escapes the ampersand a second time. Per instructions this is the known systemic bug, flagged here as an additional confirmed surface.
  - fix: Fix at the systemic source (un-escape-once or stop double-escaping in the data layer) rather than per-screen; the specialty split/render at coachCard:206 and renderProfile:465 are downstream symptoms. Verify the same fix resolves langBadges and headline.
  - file: `app/lib/scr-marketplace.ts → coachCard specialties (line 206), renderProfile specialties (line 465) — root fix in the data layer (DB.marketplace source)` · effort S · conf 0.8
- [MEM-01] **medium** · eliminate · _membership_
  - problem: The Elite tier card is fully disabled — 'Próximamente', price 'Muy pronto', a locked 'Próximamente' button, and FOUR muted feature bullets ('Todo Pro', 'Créditos de coaching incluidos', 'Acceso temprano a torneos', 'Contenido premium') that promise nothing buyable. It occupies a full third of the pricing grid (tierCards(), eliteCard, line ~544) and competes visually with the only two actionable plans (Free, Pro).
  - fix: Remove the full Elite card from the g-3 grid; if the roadmap signal matters, collapse it to a single one-line teaser strip below the two real plans ('Elite — créditos de coaching y torneos, próximamente. Avísame'), with no fake price and no feature list. This also lets Free+Pro use a 2-up grid where Pro reads as the clear recommendation.
  - file: `app/lib/scr-lifetime.ts — tierCards() eliteCard (line ~544) and the g-3 wrapper (line ~558)` · effort S · conf 0.82
- [LIFE-03] **medium** · a11y · _lifetimeProfile_
  - problem: In skillRows() the score number for skills below 60 is colored var(--warn) = #C8920C on a white card (scr-lifetime.ts line 178). #C8920C on #FFF is ~3.6:1, below the 4.5:1 AA minimum for this small (13.5px) tabular text. tokens.css line 19 itself flags --otr-gold-lo/#C8920C as 'NO texto sobre pálido: 2.4:1' — the same hue family is being used as text here.
  - fix: Use a darker token for the sub-60 score text — --warn-text / a #7c5614-class value (the codebase uses --otr-gold-text #5A4206 for gold-on-pale; for text-on-white pick something ≥4.5:1 such as #8a5a00 or reuse --text). Keep the warn hue only on the bar/icon, not the readable digits.
  - file: `app/lib/scr-lifetime.ts — skillRows(), .cr-score inline color ternary (line ~178)` · effort S · conf 0.8
- [MEM-02] **medium** · simplify · _membership_
  - problem: Plan status is stated three times on one short screen: the hero H2 'Tu plan: OTR Free', the hero 'Plan vigente' badge, and the 'En este plan desde…' line (membershipHero, lines ~488-501), then reinforced again by the disabled 'Tu plan actual' button inside the tier card. The hero also carries a sales sentence ('Tu plan decide cuánto entrenas…') that competes with the tier cards which make the same point concretely.
  - fix: Shrink the hero to one line of identity ('Tu plan: OTR Free · desde octubre 2025') and drop either the 'Plan vigente' badge or the prose sentence (the cards sell the value). Reserve the eye-catching dark treatment for the Pro card, not the status hero.
  - file: `app/lib/scr-lifetime.ts — membershipHero() (line ~488)` · effort S · conf 0.7
- [LIFE-04] **medium** · merge · _lifetimeProfile_
  - problem: The Public Profile toggle — the one privacy-sensitive, consequential control on the page — is the last card in the right rail, below Performance and Credentials (S.lifetimeProfile.render(), line ~409-412). On mobile the rail stacks last, so 'share my whole profile publicly' lands at the very bottom of a long scroll, after the journey timeline. There is also no in-page anchor or section nav.
  - fix: Either move publicProfileCard to the top of the right rail (above performance/credentials) so the privacy control is reachable first, or surface a small 'Perfil público: Activo/Privado' status chip in the identityHero with an anchor link to the card. Low effort, high payoff for the consent UX.
  - file: `app/lib/scr-lifetime.ts — S.lifetimeProfile.render() rail order (line ~409) and identityHero() (line ~91)` · effort S · conf 0.62
- [COACH-04] **medium** · eliminate · _teacher_
  - problem: The teacher screen stacks THREE distinct full surfaces on one route: (1) group tracking (4 KPIs + roster table + risk rail + pending-grading card), then (2) a SECOND four-KPI strip (Cursos/Módulos/Lecciones/Exámenes) plus (3) a read-only course tree that duplicates what 'manage' / courseBuilder already own. That is 8 KPI tiles and two page-heads on a single scroll. The course tree here is non-editable (its only real actions are quiz/video, which also live in the builder), so it is largely a redundant preview of the manage cluster.
  - fix: Remove the managePanel block from S.teacher entirely and keep teacher as a pure tracking dashboard. Move the 4 content-count KPIs (Cursos/Módulos/Lecciones/Exámenes) to the top of S.manage where they belong. The 'Editor completo' and 'Recurso (archivo)' buttons should live in manage's header. This eliminates ~65 lines and one of the two competing page identities.
  - file: `app/lib/scr-teacher.ts (S.teacher.render line 132 call; S.teacher.managePanel lines 136-200) → relocate KPIs to app/lib/scr-extra.ts S.manage.render` · effort M · conf 0.7
- [COACH-05] **medium** · simplify · _courseBuilder_
  - problem: The courseBuilder hero crams six controls into one row: back-link 'Mis cursos', save-chip, 'Modo edición: ON/OFF', 'Publicar curso / Pasar a borrador', 'Configuración', and 'Vista previa (como alumno)'. The primary objective of this screen — build the course (add sections/activities) — has NO primary button in the hero; the real primary action ('Añadir actividad') is buried per-section below. The 5-second test fails: the eye lands on a wall of equally-weighted ghost/soft buttons.
  - fix: Demote and group: keep ONE prominent primary in the hero ('+ Añadir sección' when sections exist, promoting the bottom action upward), collapse 'Configuración' + 'Vista previa' into a quiet overflow/secondary cluster, and drop the standalone publish toggle (it is redundant with Configuración → Estado) OR remove Estado from the Configuración modal and keep only the hero toggle — pick one publish surface, not two.
  - file: `app/lib/scr-extra.ts (S.courseBuilder hero lines 273-279); de-dup publish vs app/components/Aula.tsx openEditCourse line 661-662` · effort M · conf 0.7
- [COACH-06] **medium** · a11y · _teacher_
  - problem: None of the four screens emit a real top-level heading. The visible page title is <div class="page-title"> (a styled div, app.css:116), and the eyebrow above it is a <p>. Inside the page, cards jump straight to <h3>/<h4> (e.g. 'Requieren atención' h3, empty-state h4) with no <h1>/<h2> ancestor. Screen-reader heading navigation (a primary AT shortcut) finds no document title and a broken outline; the teacher screen additionally has TWO 'page-title' divs (Seguimiento del grupo AND Estructura del curso) so even visually there is no single H1.
  - fix: Promote the per-screen page-title div to a real <h1> (keep the class for styling), and ensure exactly one per screen. Verify [A11Y-*] markers elsewhere actually rely on a heading being present. For the teacher screen, after COACH-04 removes the second page-head, only one title remains — resolving the dual-H1 issue automatically.
  - file: `app/lib/scr-teacher.ts (lines 69, 183) and app/lib/scr-extra.ts (lines 215, 263); style hook app/styles/app.css:116` · effort M · conf 0.78
- [COACH-07] **medium** · merge · _participants_
  - problem: Each participant row carries three competing action affordances of three different visual weights: 'Adjudicar' (btn-soft), 'Evaluar' (btn-ghost) and a 30x30 icon-only message button. The icon button is below a comfortable touch target and its only label is a title tooltip (invisible to touch/AT). Adjudicar (rubric → moves Glicko rating) and Evaluar (eval-skills) are both heavyweight authoring actions surfaced at equal prominence on every row, creating per-row choice overload across the whole roster.
  - fix: Collapse the two rare authoring actions into a single row overflow menu ('•••' → Adjudicar ronda / Evaluar skills), keeping at most one inline primary if any. At minimum give the message icon-button an aria-label and bump it to >=36px to match --ctrl-h. Apply the same overflow pattern consistently across roster tables.
  - file: `app/lib/scr-teacher.ts (S.participants.render studentRow lines 612-616)` · effort M · conf 0.65
- [CW-04] **medium** · fix-bug · _coachwork › Disponibilidad (add slot)_
  - problem: addBtn handler only validates endMin > startMin. It does not check the new franja against existing ones, so a coach can add a duplicate or overlapping window (e.g. two Lunes 4–6 PM, or 4–6 plus 5–7). The list then shows redundant/overlapping rows and the marketplace booking surface inherits the ambiguity.
  - fix: In the #cw-av-add handler in scr-coachwork.ts, before calling the API, scan p.availability for same-weekday overlap (startMin < existing.endMin && endMin > existing.startMin) and toast a warn ('Esa franja se solapa con una existente') instead of posting. Mirror this server-side if not already enforced.
  - file: `app/lib/scr-coachwork.ts › mount() #cw-av-add click handler (lines ~526-549)` · effort M · conf 0.78
- [CW-05] **medium** · a11y · _coachwork › all tabs_
  - problem: The internal tab bar (subTabs) renders plain <button class="tab"> with only data-cw-tab and a CSS .active class. There is no role="tablist"/role="tab", no aria-selected, no aria-controls pointing at #cw-body, and no roving tabindex. The active tab is communicated to AT only by color (.tab.active → --action-hover), which is also a contrast-only signal. Same drift exists in scr-debate.ts (shared pattern).
  - fix: In subTabs(), add role="tablist" on the .tabs container and role="tab" + aria-selected="true|false" + aria-controls="cw-body" on each button; give #cw-body role="tabpanel". Since scr-debate shares the exact pattern, consider a small shared helper in components.ts to fix both centrally.
  - file: `app/lib/scr-coachwork.ts › subTabs() (lines ~45-50) + #cw-body in S.coachwork.render` · effort M · conf 0.82
- [CW-06] **medium** · fix-bug · _coachwork › all tabs_
  - problem: Tab state lives in a global window.__cwTab that is never reset on screen entry and is not reflected in the URL/route. If a coach opens 'Ingresos', navigates away (window.go elsewhere), then returns to coachwork via nav, the screen silently reopens on Ingresos rather than the default Agenda — there is no breadcrumb/URL cue explaining why. The global is also a collision risk shared across the SPA lifetime.
  - fix: Either reset window.__cwTab to 'agenda' when the coachwork screen is (re)mounted from the nav, or persist the tab in the route/hash so the header/crumbs reflect it. Lowest-effort: in S.coachwork.mount, if entering via a fresh navigation (not an internal repaint), default the tab.
  - file: `app/lib/scr-coachwork.ts › activeTab()/mount() (lines ~41-44, 403-419)` · effort M · conf 0.6
- [PARENT-04] **medium** · simplify · _parentPortal_
  - problem: Two selects on the same screen use different base classes and sizing. The report child selector uses class="input" (L227) while the per-child threshold selector uses class="select" (L338) and additionally overrides the design-system control height by setting style="...padding:5px 26px 5px 10px", which drops the standard --ctrl-h (36px) height and the .select dropdown-chevron background. The result is one full-width 36px control and one ad-hoc shrunk control in the same view.
  - fix: Standardize on class="select" for both dropdowns in scr-parent.ts; drop the inline padding override on the threshold select (L338) and instead let it size via the design-system rule, using a width modifier class if a narrower control is needed rather than killing the height.
  - file: `app/lib/scr-parent.ts (threshold select L338; report select L227)` · effort S · conf 0.82
- [PARENT-05] **medium** · fix-bug · _parentPortal_
  - problem: The threshold-change handler (L577-605) shows a success toast but provides no inline confirmation on the control itself, AND on failure it does not revert the <select> to its previous value — the dropdown keeps displaying the option the user picked even though the backend rejected it and local state was never updated. The parent is left believing 'Auto hasta $50' is active when it is not.
  - fix: In the catch block of the data-threshold-child handler (scr-parent.ts L599-601) capture the pre-change value before the await and restore sel.value to it on failure, or call repaint() so the select re-derives from thresholdValueFor(child). A toast alone is insufficient for a financial control.
  - file: `app/lib/scr-parent.ts data-threshold-child handler L577-605` · effort S · conf 0.8
- [PARENT-06] **medium** · content · _parentPortal_
  - problem: The page carries heavy redundant reassurance copy. The hero sub 'El progreso real de tu hijo/a, cada sesión segura y cada peso bajo tu control' (L392), the Security card's three bullets (L431-435), the pending-approvals subtitle 'Ninguna sesión con coach se confirma sin tu aprobación' (L419), the Messages card subtitle, and the Membership threshold helper all re-state the same 'everything is safe / you approve everything / payments in escrow' message three to four times across the screen.
  - fix: Cut the pending-approvals subtitle (L419) since the Security card already states the same guarantee; trim the threshold helper (L351) to one short line; keep the Security bullets as the single source of the trust message. Target ~40-50% reduction of the reassurance copy across the screen.
  - file: `app/lib/scr-parent.ts (L419 subtitle; L351 helper; hero sub L392)` · effort S · conf 0.7
- [PARENT-07] **medium** · redesign · _parentPortal_
  - problem: There are two ways to add a child and they are far apart and asymmetric: the empty state embeds the full link form (L363), but once children exist the link form only lives at the very BOTTOM of the right rail as the 6th card 'Vincular otro estudiante' (L458-461) — below membership and the report. A parent who just linked one child and wants to add a sibling must scroll past everything to find it.
  - fix: Promote the 'Vincular otro estudiante' affordance: either move the link card to the top of the right rail, or add a compact 'Vincular hijo/a' button into the page-head / next to the 'Hijos vinculados' KPI in scr-parent.ts (head L388-393 / KPI row L406-411) that scrolls to or opens the form.
  - file: `app/lib/scr-parent.ts (rail link card L458-461; head L388-393)` · effort M · conf 0.68
- [ADM-04] **medium** · a11y · _adminConsole_
  - problem: The destructive 'Suspender al usuario' uses an inline two-tap arm pattern: first tap rewrites the same button's text to '¿Confirmar suspensión? Tocar de nuevo' and a 4s timeout reverts it (suspendUser L215-239). There is no aria-live region, so a screen-reader user never hears that the button is now armed or that it disarmed; the confirmation is purely visual. Compounding this, the very long confirm string in a small btn-sm with no min-width causes the button to reflow/jump, shifting the card layout.
  - fix: Announce arm/disarm via an aria-live="polite" status node (or set aria-pressed + a visually-hidden live message), and give the suspend button a fixed min-width so the confirm text doesn't reflow the row. Preferred: use the existing toast()/confirm convention instead of in-button text. File: app/lib/scr-admin.ts symbol suspendUser (L215-239).
  - file: `app/lib/scr-admin.ts (suspendUser, L215-239)` · effort M · conf 0.78
- [ADM-05] **medium** · fix-bug · _adminUsers_
  - problem: On the admin's OWN row the role <select> still lists non-admin options and the Suspender button is fully enabled, but the backend rejects both (route.ts L94 'No puedes quitarte tu propio rol' and L99 'No puedes suspenderte a ti mismo'). The UI invites an action it knows will fail, surfacing the guard only as a red error toast after the round-trip.
  - fix: Pass the current admin id into render (state likely has it), and in userCard disable+title the role <select> options other than ADMIN and disable the Suspender button when u.id === me, with a small 'Tú' badge for orientation. File: app/lib/scr-admin-users.ts symbol userCard (L47-88) + render() to thread the self id.
  - file: `app/lib/scr-admin-users.ts (userCard L47-88, render L118)` · effort M · conf 0.72
- [ADM-06] **medium** · simplify · _adminUsers_
  - problem: Search requires an explicit click on 'Buscar' (or Enter) and there is NO way to clear it: once st.q is set, the input keeps the term and the only reset is manually deleting the text and searching again on an empty string. The empty-results state ('Sin resultados / Ningún usuario coincide') gives no 'Limpiar búsqueda' affordance (viewBody L104-111). Combined with role chips, an admin can land in a filtered+searched dead-end with no one-tap escape.
  - fix: Add a 'Limpiar' (x) button in the search row that resets q (and optionally role) and reloads, and add a 'Limpiar búsqueda' button inside the no-results empty state. File: app/lib/scr-admin-users.ts render() search row (L155-161) + viewBody empty branch (L104-111).
  - file: `app/lib/scr-admin-users.ts (render L155-161, viewBody L104-111)` · effort S · conf 0.8
- [ADM-07] **medium** · a11y · _adminUsers_
  - problem: None of the interactive controls have programmatic labels. The per-row role <select> (L52) has only a nearby small <span>Rol</span> that is not associated via for/id or aria-label; the search <input id="au-search"> (L157) relies on placeholder only with no <label>; verify/suspend buttons convey meaning by text but there is no per-user context for AT (a screen reader hears 'Suspender' with no indication of which user). The file has zero aria-* or <label> markup (confirmed by grep).
  - fix: Add aria-label to each control including the user name, e.g. role select aria-label=`Rol de ${u.name}`, suspend aria-label=`Suspender a ${u.name}`, verify aria-label=`Verificar a ${u.name}`; give #au-search an aria-label 'Buscar usuarios' (or a visually-hidden <label for>). File: app/lib/scr-admin-users.ts symbol userCard (L51-62) + search input (L157).
  - file: `app/lib/scr-admin-users.ts (userCard L51-62, search L157)` · effort S · conf 0.85
- [PROF-03] **medium** · redesign · _coach_
  - problem: starsRO() (scr-profile.ts line 12) and the clickable review .star buttons (line 463) render rating stars in `color:var(--otr-sky)` which tokens.css aliases to the brand GREEN #2CAA20. Per the OTR brand book and tokens.css comments, green is reserved for ACTION/interactive and gold (--otr-gold #F2B814) is the LOGRO/achievement color. Star ratings are an achievement/quality signal and are universally rendered gold/amber. Green stars are both a token-semantics violation and a convention break.
  - fix: Change starsRO fill and the review .star button color from var(--otr-sky) to var(--otr-gold) (and the lit/.on state likewise) in scr-profile.ts (lines 12 and 463). This aligns rating UI with the achievement token and with the gold already used for the certificate seal and badge medals in the same file.
  - file: `app/lib/scr-profile.ts (starsRO line 12; .star button color line 463)` · effort S · conf 0.8
- [PROF-04] **medium** · eliminate · _coach_
  - problem: renderCoachSelf shows the coach's rating and review count THREE times on one screen: in the hero inline row (line 210-211), in the hero KPI strip (lines 222-223, Rating + Reseñas), and again in the right-rail 'Valoración general' card (lines 254-256). The student-facing coach screen repeats it twice (hero row + 'Valoración' card). The same two numbers are restated with no added information.
  - fix: Drop Rating and Reseñas from the hero KPI strip in renderCoachSelf (scr-profile.ts lines 222-223) — keep only Programas there — and let the right-rail 'Valoración general' card own the rating/review summary. Optionally remove the inline stars+count from the hero row too, since the rail card already shows them.
  - file: `app/lib/scr-profile.ts (renderCoachSelf hero KPI strip lines 221-223)` · effort S · conf 0.75
- [PROF-05] **medium** · a11y · _coach_
  - problem: The review rating is a row of 5 individual <button class="star"> elements (scr-profile.ts line 463). Each has an aria-label ('N estrellas') but the group has no radiogroup/label semantics and the buttons carry no aria-pressed; the selected rating exists only as a .on CSS class. A screen-reader user gets five unrelated buttons, no announced current value, and no indication that picking one is required. On submit, validation is a visual toast ('Selecciona una calificación', Aula.tsx leaveReview line 753) with no aria-live, so the error is not announced.
  - fix: Wrap #review-stars in role="radiogroup" aria-label="Calificación" and give each .star role="radio" with aria-checked toggled alongside the .on class (update the toggle in Aula.tsx ~line 938 and mount() in scr-profile.ts ~line 421). Ensure the validation toast renders into an aria-live="assertive" region.
  - file: `app/lib/scr-profile.ts (#review-stars / .star buttons line 462-463; mount paint() line 421) + app/components/Aula.tsx (star toggle line 938, leaveReview toast line 753)` · effort M · conf 0.8
- [CERT-02] **medium** · simplify · _certificate_
  - problem: scr-certificate.ts references three CSS classes that are not defined anywhere in app/styles/*.css: .cert-seal (the ceremonial seal wrapper, line 14), .cert-name (the recipient name, line 86), and .diploma (the card, line 62). The seal and name therefore render only with their inline styles; the .cert-seal wrapper has no layout/centering rules of its own, so the seal's placement is entirely incidental. This is unmaintained component drift — anyone editing the diploma layout via CSS will find no rules to edit.
  - fix: Either (a) add real definitions for .cert-seal (display:flex;justify-content:center;margin:0 auto), .cert-name, and .diploma in screens.css, co-located with the new @media print rules from CERT-01, or (b) drop the orphan class names from scr-certificate.ts and keep the inline styles as the single source of truth. Pick one; do not leave dangling class hooks.
  - file: `app/styles/screens.css + app/lib/scr-certificate.ts (otrSeal, cert-name, .diploma)` · effort S · conf 0.85
- [SET-01] **medium** · content · _settings_
  - problem: Notification preferences (the 4 toggles in NOTIF) persist ONLY to localStorage (notifOn/toggle handlers, lines 23-31, 127-136) and the toast says 'Notificación activada' as if it were saved to the account. There is no disclosure that these settings are device-local and will not follow the user to another browser/device, nor that they do not actually gate any backend notification. The leaderboard toggle right beside them DOES hit PATCH /api/profile, so two visually identical switches have completely different persistence semantics with no signal to the user.
  - fix: In scr-settings.ts add one faint line under the Notificaciones card title, e.g. 'Estas preferencias se guardan en este dispositivo por ahora.', and soften the toast to 'Preferencia guardada en este dispositivo'. Longer term, route these through PATCH /api/profile like the leaderboard toggle so the two switches behave identically. Lowest-effort honest fix is the disclosure line.
  - file: `app/lib/scr-settings.ts (NOTIF card in render, toggle mount handler toast)` · effort S · conf 0.8
- [EVT-01] **medium** · simplify · _events_
  - problem: The entire 'Torneos' section is a non-owning mirror of the Debate Hub: tournamentRow's only interactive control is 'Ver e inscribirme →' (data-go-tournaments) and the section footer is another 'Ver todos en el Debate Hub →' — both route away to debate. The file's own header comment confirms tournament enrollment 'NO se duplica: vive en el Debate Hub'. So the screen shows a read-only list whose every action bounces the user to another screen to actually act. That is a redirect surface masquerading as a destination.
  - fix: In scr-events.ts collapse the Torneos card to a single compact teaser: show the count + the next 1-2 tournament names and ONE 'Ver torneos en el Debate Hub →' link, dropping the per-row 'Ver e inscribirme' buttons (they all go to the same place anyway). Let Events be 'próximos eventos' first-class and tournaments a one-line pointer. Keeps the screen but removes the duplicated action rows.
  - file: `app/lib/scr-events.ts (tournamentRow + tournamentsSection in S.events.render)` · effort S · conf 0.7
- [ROOM-01] **medium** · merge · _room_
  - problem: The session-detail aside prints the raw uppercase enum for status: `<span>${esc(b.status)}</span>` (line 129) renders 'CONFIRMED' / 'PENDING' / 'COMPLETED' to the user. Everywhere else in the cluster the same statuses are humanized — scr-mybookings.ts statusBadge maps CONFIRMED→'Confirmada', PENDING→'Esperando consentimiento', etc. So the exact same booking shows a friendly Spanish badge in 'Mis reservas' and a raw English code in the room it links to.
  - fix: In scr-room.ts import and reuse the statusBadge() helper from scr-mybookings.ts (or extract it to a shared helper) and render that in the Estado row instead of esc(b.status). At minimum, map the enum to the same Spanish labels used by statusBadge.
  - file: `app/lib/scr-room.ts (aside kv block, Estado row) reusing scr-mybookings.ts statusBadge` · effort S · conf 0.8
- [MB-01] **medium** · redesign · _myBookings_
  - problem: Cancel is a destructive, money-moving action (PATCH action:'cancel' → escrow REFUNDED) but the only guard is a 'tap the button again within 4s' arm pattern (upcomingRow line 90; mount lines 184-209). The button label changes to '¿Cancelar? Tocar de nuevo'. There is no modal stating the consequence (refund, slot lost), no reason capture, and the double-tap is easy to fire accidentally on touch (two quick taps). It is also shown for PENDING bookings the same way as CONFIRMED, with no distinction in stakes.
  - fix: In scr-mybookings.ts replace the data-armed double-tap with the existing confirm modal (the codebase already has w.otrFormModal / a confirm dialog used elsewhere): show 'Cancelar esta sesión? Se reembolsará tu pago y se liberará el horario.' with explicit Confirmar/No. Keep the optimistic refresh on success. This aligns it with how other destructive flows are guarded.
  - file: `app/lib/scr-mybookings.ts (S.myBookings.mount data-mb-cancel handler)` · effort M · conf 0.7
- [MSG-01] **medium** · fix-bug · _messages_
  - problem: Switching conversations does a full screen re-render: clicking a .convo sets window.__convo and calls go('messages') (mount lines 142-145), which rebuilds the entire msg-wrap. This drops the scroll position you were in, re-runs all fade-up animations, blurs the compose input, and re-fetches/rebuilds the whole list for what should be an in-place panel swap. Sent messages are optimistically appended with when:'ahora' but there is no delivered/read/sending state and no error surface if the POST fails (the fetch .catch is a no-op, line 155).
  - fix: In scr-community.ts S.messages.mount, on .convo click swap only the .msg-thread innerHTML (re-run the thread builder for the new active index) instead of go('messages'); preserve compose focus. For send, surface a failure: on fetch .catch, mark the bubble with an error state + retry instead of swallowing. Also note this screen lives in scr-community.ts which is otherwise the OFF/disabled cluster — consider moving S.messages to its own file so the live messages route is not coupled to disabled forum code.
  - file: `app/lib/scr-community.ts (S.messages.mount — convo click handler + push() error handling)` · effort M · conf 0.65
- [OB-01] **medium** · simplify · _onboarding_
  - problem: The onboarding footer presents two competing exits with near-equal weight: a 'Saltar' ghost button and the primary 'Guardar y entrar al hub' — both ultimately navigate to dashboard (ob-foot, lines 310-313). For a first-time user this muddies the single intended path. Additionally the student goals chips are bound twice with different state models: render() marks active via prefs.goals.includes (line 303) but mount() toggles purely with classList.toggle on click and then reads .chip.active on save (lines 320, 334) — workable but the 'Saltar' path discards everything silently with no acknowledgement that nothing was saved.
  - fix: In scr-hub.ts onboarding.render, demote 'Saltar' to a quiet text link aligned left (or 'Saltar por ahora' as a small btn-quiet) and let only 'Guardar y entrar' carry primary weight; keep them apart (Fitts/grouping). Optionally add a toast on Saltar ('Puedes configurarlo luego en Ajustes') so the skip isn't a silent dead-end.
  - file: `app/lib/scr-hub.ts (onboarding.render ob-foot buttons)` · effort S · conf 0.6
- [PL-01] **medium** · a11y · _placement_
  - problem: The 6 placement sliders are native range inputs styled ONLY with inline `accent-color:var(--otr-sky)` (scr-placement.ts line 59); there is NO .pl-range CSS anywhere in app/styles (confirmed by grep — .pl-range, .pl-out are undefined). So the draggable thumb is the browser default size (~12-16px) with no enlarged hit area, no visible custom focus ring matching the app's --ring, and inconsistent rendering across browsers. This is the single most important interaction on the screen (the whole task is dragging these), yet it gets the least design attention and the smallest target.
  - fix: Add .pl-range styling in app/styles/screens.css: enlarge the thumb (e.g. 22-24px) with ::-webkit-slider-thumb / ::-moz-range-thumb, a clear track, and a :focus-visible box-shadow:var(--ring) so keyboard focus is visible and consistent with the rest of the app. Also style the .pl-out value chip so the live readout reads clearly.
  - file: `app/styles/screens.css (new .pl-range / .pl-out rules) for scr-placement.ts sliders` · effort M · conf 0.7
- [IA-03] **medium** · fix-bug · _Cross-role route reachability (Aula.tsx renderApp guard)_
  - problem: renderApp() only role-gates routes that carry an explicit role field (Aula.tsx line 57). Student-shaped routes — progress, lifetime, badges, grades, course, catalog, debate, placement, certificate, membership, my-bookings — have NO role field, so a coach/parent/admin reaching them (via search, a stray go(), or a hand-edited #hash) renders student-only screens (XP/streak/skill-graph 'Niveles', 'Mi trayectoria', 'Mis calificaciones') inside their own shell. These concepts explicitly don't belong to coach/parent (shell.ts lines 66-70 say Niveles is not the coach's concept), yet nothing prevents arrival.
  - fix: Tag the student-only routes in app/lib/screens.ts with role:'student' (progress, lifetime, badges, grades, course, catalog, placement, certificate, plus the student-only course sub-routes), so renderApp's existing guard (Aula.tsx line 57) redirects non-students to ROLE_HOME. Keep shared routes (membership, explore/marketplace, messages, settings, profile, dashboard, events, debate where intended) ungated.
  - file: `app/lib/screens.ts (add role:'student' to student-only ROUTES); guard already in app/components/Aula.tsx line 57` · effort M · conf 0.78
- [IA-04] **medium** · fix-bug · _Unknown-route navigation (Aula.tsx renderApp)_
  - problem: renderApp() does `let def = ROUTES[r]; if (!def) return;` (Aula.tsx lines 53-54) — clicking a link to a non-existent route does nothing at all: no toast, no 404 surface, no console hint. Live, this is reachable from disabled screens that still emit dead links (scr-hub.ts data-go="arsenal", scr-community.ts go('forum')/go('forum-thread')); those screens are OFF by design, but the silent-no-op behavior is the actual IA risk because any future typo'd route or stale link dead-ends invisibly.
  - fix: In app/components/Aula.tsx renderApp, replace the bare `if (!def) return;` with a graceful fallback: in dev console.warn the missing route, and for the user either toast('Esa sección no está disponible') and stay put, or redirect to ROLE_HOME[state.role]. This guarantees navigation always produces visible feedback.
  - file: `app/components/Aula.tsx (renderApp, lines 52-54)` · effort S · conf 0.7
- [DS-02] **medium** · merge · _Settings, Lifetime (Mi trayectoria)_
  - problem: The on/off switch is hand-coded with inline styles in 3 places and there is no shared component or class in app.css. scr-settings.ts:28-31 (toggle) and :35-38 (lbToggle) are VERBATIM copies of each other, and scr-lifetime.ts:372 is a third variant. Worse, the colors drift: settings toggles use 'var(--otr-green)' (#2CAA20, bright) for the 'on' state, while lifetime's toggle uses 'var(--otr-sky-lo)' (#1E8C16, the dark green) for the identical 'on' semantic. Same control, two different greens, depending on which file rendered it.
  - fix: Add a single .switch component to app.css (track 44x25, thumb 19px, radius var(--r-pill), on=var(--otr-green), off=var(--n-200), role=switch already present) and a tiny shared switchHtml(key,on,label) helper. Replace scr-settings.ts:28 toggle, :35 lbToggle, and scr-lifetime.ts:372 with it. This also locks the 'on' color to one token.
  - file: `app/styles/app.css (add .switch) + app/lib/scr-settings.ts:28,35 + app/lib/scr-lifetime.ts:372` · effort M · conf 0.92
- [DS-03] **medium** · simplify · _Global (secondary actions across all roles)_
  - problem: btn-soft (green tint, 54 uses) and btn-ghost (white + border, 48 uses) are applied to the SAME secondary-action role with no discernible rule. Examples within one screen-cluster: 'Ver más coaches' = btn-ghost but 'Explorar coaches' = btn-soft; 'Ver índice' = btn-ghost but 'Ver progreso'/'Ver programa'/'Ver historial' = btn-soft; the 'Volver' back-action is rendered as btn-ghost in some places (scr-marketplace 'Volver al curso', course-builder examples) and btn-soft in others. The choice is author-by-author, not semantic.
  - fix: Write a one-line rule in app.css above the .btn block and apply it: btn-soft = the single suggested secondary action on a surface (the one you'd nudge toward); btn-ghost = neutral/back/dismiss and any repeated row action. Concretely, standardize all 'Volver'/back buttons to btn-ghost and reserve btn-soft for the primary 'explore/continue' secondary CTA. Codemod: most 'Volver' instances are btn-soft btn-sm and should become btn-ghost btn-sm.
  - file: `app/styles/app.css (.btn-soft/.btn-ghost doc) + scr-core.ts/scr-learn.ts/scr-marketplace.ts/scr-debate.ts back-button instances` · effort M · conf 0.78
- [A11Y-04] **medium** · a11y · _Badges/status indicators (global) — badge.ok, badge.danger, eyebrow, active tab_
  - problem: Several small-text-on-tint token pairs fall below the 4.5:1 AA threshold for normal/bold text. Measured from tokens.css values: .badge.ok (#1E8C16 on #E1F2DE) = 3.73:1; .badge.danger (#C2453C on #F7E0DE) = 3.96:1; .eyebrow (green #2CAA20, 11px bold) = 3.05:1 (77 uses); .tab.active label (--action-hover #1E8C16 on white) = 4.36:1; .sky text class (#2CAA20) = 3.05:1 (15 text uses). All are <14pt or bold-<14pt, so the 4.5:1 normal-text rule applies, not the 3:1 large-text rule.
  - fix: In app.css/tokens.css: set .badge.ok text to a darker green (use --otr-green-text #176B11 → 5.71:1, matching .badge.sky), darken .badge.danger text to ~#9A2F28 (≈5:1 on its soft), set .eyebrow and the .sky text class to #176B11 instead of #2CAA20, and set .tab.active color to the darker --ok/#176B11. No layout impact — these are color-only swaps reusing existing tokens.
  - file: `app/styles/app.css (.badge.ok L202, .badge.danger L204, .eyebrow L118, .tab.active L232, .sky L24)` · effort S · conf 0.85
- [SIMP-02] **medium** · eliminate · _Dashboard (student)_
  - problem: The dashboard Achievements card (scr-core.ts:291-309) ends with a button that is a byte-for-byte copy of the hero's primary CTA: onclick="${na.onclick}">${na.ic} ${na.cta} — the same 'Continuar lección' / 'Reservar sesión' action already shown at the very top of the same page in the hero (scr-core.ts:125). The page therefore shows the identical primary action twice within one scroll. The accompanying comment ([CNV-05]) justifies it as 'closing the gamification gap', but it duplicates the single most prominent button on the screen.
  - fix: Replace the duplicated CTA in the achievements card (scr-core.ts:308) with the card's natural destination — 'Ver logros' → go('lifetime') (or go('badges') until SIMP-01 lands). Keep the XP-to-next bar and next-badge label; drop the second copy of the hero action so the hero owns the single primary CTA.
  - file: `app/lib/scr-core.ts (S.dashboard.render, achievements block, line 308)` · effort S · conf 0.8
- [SIMP-03] **medium** · eliminate · _Events (student/coach/parent/admin)_
  - problem: The Events screen (scr-events.ts) has no action it owns. Its 'Torneos' section explicitly routes every CTA to the Debate Hub ('Ver e inscribirme' and 'Ver todos en el Debate Hub' both fire data-go-tournaments → go('debate'), scr-events.ts:41,66,77-78) and its file header states 'La inscripción a torneos NO se duplica: vive en el Debate Hub'. Its 'Próximos eventos' list is the same DB.events the dashboard right-rail already shows as 'Próximas sesiones' fallback (scr-core.ts:250-256). So Events is a read-only mirror of two surfaces with a redirect button.
  - fix: Either (a) eliminate the Events nav item and fold 'seminars/live sessions' into the dashboard upcoming rail + tournaments into Debate Hub (lowest-cost, highest-clarity), OR (b) if Events must stay per PRD, give it the ONE thing it lacks — actual seminar/workshop RSVP that exists nowhere else — so it earns its slot. Do not keep it as a pure redirect. Recommend (a): remove the events entry from NAV.student 'group.main' (shell.ts:23) and let go('debate') own tournaments.
  - file: `app/lib/shell.ts (NAV.student group.main, line 23) + app/lib/scr-events.ts (S.events)` · effort M · conf 0.66
- [SIMP-04] **medium** · merge · _Coach: Panel de coach (teacher) + Mis cursos (manage) + Constructor de curso (course-builder)_
  - problem: The coach has THREE overlapping course-structure surfaces. S.teacher.managePanel (scr-teacher.ts:136-200) renders the full Cursos→Módulos→Lecciones→Examen tree read-mostly with quiz/video buttons AND a 4-KPI strip (Cursos/Módulos/Lecciones/Exámenes). S.manage (scr-extra.ts:212-242) renders the same course list as cards that each open the builder. S.courseBuilder (scr-extra.ts:248-291) renders the same tree again, editable. The managePanel even links to S.manage via 'Editor completo' (scr-teacher.ts:187), and S.teacher's own page-head 'Calificar' + this embedded panel make the teacher dashboard do double duty as a course manager.
  - fix: Demote S.teacher.managePanel from a full structure tree to a single compact 'Tus cursos' summary card (count + 'Ir a Mis cursos' button) so the teacher dashboard is purely tracking (roster + at-risk + grading queue). Let manage (list) + course-builder (edit) be the one authoring path. Removes a ~65-line duplicated tree + a redundant 4-KPI grid from scr-teacher.ts.
  - file: `app/lib/scr-teacher.ts (S.teacher.managePanel, lines 136-200)` · effort M · conf 0.7
- [SIMP-05] **medium** · automate · _Lesson / Assignment / Quiz (student learning flow)_
  - problem: The 'mark lesson complete' step is a manual action the system already has the signal to automate in two of three activity types. On a quiz, passing it already returns res.courseProgress and marks the lesson done server-side (the results screen shows 'Lección completada', scr-learn.ts:540-541). But on a video player and a text lesson the student must separately click 'Marcar como completada' (scr-learn.ts:593, scr-core.ts:611-612) even after submitting an assignment for that same lesson (scr-learn.ts S.assignment submit) — leaving lessons that are clearly 'done' still showing as pending and blocking the auto-advance the player's 'Siguiente' relies on.
  - fix: Auto-mark a lesson complete when its terminal action succeeds: on successful /api/submissions POST for an assign/mic lesson (scr-learn.ts:349-359) flip doneByMe like the quiz flow already does. Keep the manual toggle only for pure video/text lessons that have no submission/quiz. This removes a step from the two most-common completion paths without removing user control where it's the only signal.
  - file: `app/lib/scr-learn.ts (S.assignment submit handler, ~lines 349-359) + server /api/submissions completion side-effect` · effort M · conf 0.6
- [COG-02] **medium** · simplify · _Marketplace · Coaches (marketplace/explore)_
  - problem: renderGrid() shows up to 11 filtering controls above the results: a row of up to 8 specialty chips (specs sliced to 8) plus three selects (Idioma / Precio / Orden). The user faces a wall of filter choices before seeing any coach, and specialties + sort + price overlap conceptually.
  - fix: Reduce default visible controls: keep the specialty chips but cap to ~5 with a '+N más' expander; merge Idioma+Precio into a single 'Filtros' control or keep only the two highest-value filters (specialty + price) and demote Orden to a compact right-aligned sort dropdown. Results should be visible without scrolling past the filter bar. Files: scr-marketplace.ts renderGrid() lines ~236-273 (specs = ...slice(0,8); the three <select> blocks).
  - file: `app/lib/scr-marketplace.ts (renderGrid, lines ~239-269)` · effort S · conf 0.7
- [COG-03] **medium** · simplify · _Coach · Participantes (participants)_
  - problem: Each roster row renders three actions — 'Adjudicar', 'Evaluar', and an icon-only message button — repeated for every student. The repeated 3-way decision per row multiplies choices (Hick), and the message control is a 30x30px icon-only button (icon-btn width:30px;height:30px, scr-teacher.ts:615) with only title= and no aria-label, below the 44px touch/Fitts target and weak for screen readers.
  - fix: Demote to one primary row action plus an overflow: keep 'Adjudicar' (or 'Evaluar') visible and move the rest into a kebab/'⋯' menu, OR drop the per-row message icon (messaging is reachable from the student detail). For any icon-only control add aria-label and bump to 36-44px min target. Same fix applies to the icon-only message button in S.teacher 'Requieren atención' rows (scr-teacher.ts:117) and the 30x30 message button at scr-teacher.ts:615.
  - file: `app/lib/scr-teacher.ts (S.participants.studentRow ~605-617; icon-btn line 615)` · effort S · conf 0.72
- [COG-04] **medium** · content · _Coach · Seguimiento del grupo (teacher KPIs)_
  - problem: The third KPI tile is labeled 'Engagement promedio' but is fed by k.onTime (the 'puntualidad'/on-time field). The code comment at scr-teacher.ts:79 explicitly notes the value derives from Enrollment.engagement, yet the column header in the roster below is 'Engagement' too — so two different concepts (a KPI labeled engagement-but-sourced-from-onTime, and a per-row engagement pill) sit feet apart with overlapping names.
  - fix: Make label and source agree: either rename the field/derivation so the KPI truly aggregates the same engagement scale shown in the roster pills, or rename the KPI back to what onTime measures (e.g., 'Puntualidad' / 'Entregas a tiempo'). Add a one-line tooltip stating how it's computed, consistent with the per-row pill semantics.
  - file: `app/lib/scr-teacher.ts (KPI tile line ~80, C.kpi('Engagement promedio', String(k.onTime)))` · effort S · conf 0.75

## LOW (36)
- [CORE-05] **low** · eliminate · _catalog_
  - problem: Every catalog card renders a price slot that is always 'Gratis': queries.ts forces price:0 for all courses (queries.ts:1430 `price: 0` per the membership-value PRD §13.1), so the `c.price > 0` branch in S.catalog (scr-extra.ts:191) is dead and every card shows a green 'Gratis' badge.
  - fix: Remove the price slot from S.catalog.card and replace with a more useful signal (format/modality chip, like the dashboard reco card already shows at scr-core.ts:183), or a single 'Incluido en tu membresía' note shown once on the page header rather than per card. Drop the dead price>0 branch.
  - file: `app/lib/scr-extra.ts (S.catalog.render → card foot, lines 190-195)` · effort S · conf 0.75
- [CORE-06] **low** · fix-bug · _dashboard_
  - problem: In the leaderboard-fallback 'Tu actividad reciente' block the dashboard re-escapes activity titles with esc(a.title) (scr-core.ts:332), but DB.activity.title is already esc()'d in queries.ts:1439. A title containing '&' would render '&amp;'. (The parallel S.progress 'Subidas recientes' is correct — it uses ${ev.title} without re-escaping at scr-profile.ts:126.)
  - fix: In scr-core.ts leaderboard fallback, change esc(a.title) to ${a.title} and esc(a.detail) to ${a.detail||''} to match the already-escaped contract (and the progress screen). Keep esc(a.when) only if when is raw (it is whenLabel, not esc'd — verify; queries.ts:1440 `when` is not esc'd so keep esc there).
  - file: `app/lib/scr-core.ts (S.dashboard → leaderboard fallback, line 332)` · effort S · conf 0.85
- [CORE-07] **low** · a11y · _grades_
  - problem: The grade letter and 'En revisión'/'—' states are conveyed by color+badge only, and the coach-feedback expandable row (.fb-row, scr-learn.ts:661) is a visually-associated <tr> with no programmatic link to its parent grade row. Screen-reader users hear an orphaned 'Comentario del coach' block with no binding to the activity it grades, and the A/B letter color (ok green vs sky) is the only differentiator between letter tiers.
  - fix: Give the main <tr> an id and the .fb-row an aria-label referencing the activity name (e.g. `Comentario del coach sobre ${activity}`), or restructure feedback as a <details>/disclosure inside the activity cell. Minimal: add aria-label to the fb-row alert in scr-learn.ts:661.
  - file: `app/lib/scr-learn.ts (S.grades → fbRow, lines 661-663)` · effort S · conf 0.6
- [CORE-08] **low** · a11y · _badges_
  - problem: Locked badge cards differentiate earned vs locked purely via a lock glyph + reduced opacity ('.locked' class) and a corner icon; the medal art and name remain, but there is no text status for assistive tech distinguishing 'Ganada' from a locked badge — only the earned cards get a visible 'Ganada' badge (scr-profile.ts:164), locked cards get just a lock IC with no accessible label.
  - fix: Add an accessible label to the locked corner marker in scr-profile.ts:164 (e.g. wrap the lock IC with aria-label='Bloqueada' / a visually-hidden 'Bloqueada' span), mirroring the visible 'Ganada' on earned cards.
  - file: `app/lib/scr-profile.ts (S.badges → badge-card, line 164)` · effort S · conf 0.6
- [LRN-05] **low** · fix-bug · _quizResults_
  - problem: Line 514: const tone = passed ? (pct >= 90 ? 'ok' : 'ok') : ... — both branches of the ternary return 'ok', so the intended '90%+ honors' distinction never affects the ring/badge color. The 'Aprobado con honores' text (line 536) shows but the visual tone is identical to a plain pass, so the honors moment is visually flat.
  - fix: Either collapse to const tone = passed ? 'ok' : (pct >= 50 ? 'warn' : 'danger') and drop the dead inner ternary, OR make 90%+ use the achievement token (--otr-gold) for the ring so honors reads visually. Pick one; do not ship a no-op ternary.
  - file: `app/lib/scr-learn.ts → S.quizResults.render() line 514` · effort S · conf 0.9
- [LRN-10] **low** · simplify · _assignment_
  - problem: When a coach has not configured submitKinds, the assignment shows all three submission methods stacked at once — a dark voice recorder, a file dropzone, and a free-text area — each labeled 'O…' (Or…). That is three co-equal primary inputs plus a rubric card plus a previous-entry card, presenting a wall of equally-weighted choices for what is usually one intended submission type.
  - fix: Default to a single segmented control (Grabar / Subir / Escribir) that reveals one input at a time, defaulting to the most common (record for an oratory product). Keep all three only when the coach explicitly enabled multiple. Lands in S.assignment.render() lines 95-189.
  - file: `app/lib/scr-learn.ts → S.assignment.render() submission inputs, lines 95-189` · effort M · conf 0.6
- [DEB-08] **low** · simplify · _debateHub_
  - problem: The 'Registrar un debate' action appears in three different button styles for the same action: white-on-navy in the hero (line 146), btn-primary in history header/empty (lines 219,248), and btn-soft 'Registrar resultado de práctica' in practice (line 297) which routes to the SAME openRecordDebate flow but with a different label and a hidden 'Fuente' field (isPractice forces source=OTR). A user cannot tell from the labels that 'Registrar un debate' and 'Registrar resultado de práctica' open essentially the same form.
  - fix: Standardize the primary record action to one button style (btn-primary) and keep the hero variant only if the navy panel requires the inverse treatment. Clarify that practice-registration is the same flow (e.g. a single 'Registrar' with the source preselected) rather than a separately-named action.
  - file: `app/lib/scr-debate.ts — record buttons at lines 146, 219, 248, 297` · effort S · conf 0.55
- [DEB-09] **low** · fix-bug · _debateHub_
  - problem: The PF practice timer is fragile across navigation. mountPfTimer (line 661) stores its interval on window.__pfTimer and hooks window.__recTeardown, but the active phase/remaining state lives only in mount-scoped closures (idx, remaining, running). Any repaint — including switching sub-tab and coming back, which calls repaint()->render()->mount() (lines 471-477) — destroys and recreates the timer at 0:00. A user who starts a 4-minute Constructive, peeks at the Leaderboard tab, and returns loses their running clock with no warning.
  - fix: Persist timer state on window (like __debateTab): store {idx,remaining,running,startedAt} and rehydrate in mountPfTimer so a return to the Práctica tab resumes. Alternatively warn/confirm before leaving while running. Minimal version: keep the interval alive and re-bind to fresh DOM nodes on repaint instead of resetting idx=-1.
  - file: `app/lib/scr-debate.ts — mountPfTimer (line 661-745) closure state vs repaint (line 471)` · effort M · conf 0.6
- [MKT-07] **low** · a11y · _marketplace/explore (grid)_
  - problem: When a coach has a photo, the avatar is rendered as a CSS background-image on a `<span>` with the initials set to `color:transparent` (coachCard:186). The photo conveys identity but is invisible to assistive tech (background images get no alt), and the fallback initials are deliberately hidden, so AT users get nothing for that coach's visual identity. The same pattern is absent on the profile hero, which uses C.avatar (initials) — inconsistent.
  - fix: Either render coach photos as a real `<img src alt="${esc(c.name)}">` inside the avatar, or keep the background but add `role="img" aria-label="${esc(c.name)}"` to the span and do NOT hide the initials from AT. Prefer routing photo support through C.avatar so the avatar component owns the alt contract.
  - file: `app/lib/scr-marketplace.ts → coachCard avatar branch (line 186)` · effort S · conf 0.7
- [MKT-08] **low** · a11y · _both_
  - problem: Two minor design-system contrast nits: the discount badge `.badge.ok` (used at bookingCard:384 `-${p.discountPct}%`) is #1E8C16 on #E1F2DE = 3.73:1, under AA 4.5 for its 11.5px bold text; and the `.badge.warn` consent badge text relies on --otr-gold-text which is fine, but the warn alert icon/title path should be spot-checked. The discount badge is the one that fails.
  - fix: Darken `.badge.ok` text to --otr-green-text (#176B11 → 4.6:1 on #E1F2DE) in app.css:202, or increase its font size/weight. Confirm with a contrast check after the change.
  - file: `app/styles/app.css → .badge.ok (line 202)` · effort S · conf 0.65
- [MEM-03] **low** · fix-bug · _membership_
  - problem: In tierCards() the optimistic in-flight states are inconsistent and lossy. The downgrade/upgrade buttons set btn.textContent = 'Actualizando…' (mount(), line ~611), which on the Pro button overwrites the IC.flame icon markup and on failure is restored as plain text 'Pasar a Pro' / 'Cambiar a Free' WITHOUT the icon (lines ~622). The styled white Pro button and the disabled 'Tu plan actual' state also diverge in markup, so a failed-then-retried plan switch silently degrades the button's appearance.
  - fix: Instead of mutating textContent, capture btn.innerHTML before the call and restore it on error; set aria-busy="true" during the request; or simpler, disable the button + rely on repaint() (which already rebuilds from DB) and skip the textContent dance entirely.
  - file: `app/lib/scr-lifetime.ts — S.membership.mount() data-mem handler (lines ~610-623)` · effort S · conf 0.72
- [LIFE-05] **low** · a11y · _lifetimeProfile_
  - problem: Section titles across the page are <b style="font-size:15px"> not real headings (skillGraphCard line ~199, performanceCard ~265, credentialsCard ~292, journeyCard ~339, publicProfileCard ~378). The page has a single <h2> in identityHero and then no heading structure — every card title is bold text. The empty-state cards do use <h4>, so the heading hierarchy is also internally inconsistent (h4 inside an empty card, but <b> for the populated equivalent).
  - fix: Promote each card's bold title to <h3 class="..."> (keep the visual size via class), and make the empty-state titles <h3> too for consistency. Keep the eyebrow as a <p>/<div> kicker. Purely additive, no layout change.
  - file: `app/lib/scr-lifetime.ts — skillGraphCard/performanceCard/credentialsCard/journeyCard/publicProfileCard title <b> tags` · effort M · conf 0.66
- [LIFE-06] **low** · simplify · _lifetimeProfile_
  - problem: The Ledger renders 7 KPI tiles in a 4+3 split (ledgerTiles(), lines ~217-231): Cursos completados, Lecciones terminadas, Debates competidos, Victorias, Sesiones asistidas, Torneos, Horas de coaching. Seven undifferentiated big-number tiles sit between the Skill Graph and the Journey with no grouping logic beyond 'top row / bottom row', and several overlap conceptually (Debates competidos vs Victorias vs Torneos; Lecciones vs Cursos).
  - fix: Cut or merge to the 4 highest-signal counters (e.g. Cursos, Debates, Victorias, Horas de coaching) and fold the rest into a single 'detalle' line, or label the two rows with a kicker ('Aprendizaje' / 'Competencia') so the grouping carries meaning. Verify the win-rate isn't more useful than raw Victorias.
  - file: `app/lib/scr-lifetime.ts — ledgerTiles() top/bottom arrays (lines ~217-227)` · effort S · conf 0.55
- [COACH-08] **low** · eliminate · _teacher_
  - problem: scr-teacher.ts line 8 defines trendIc — a tangled one-liner (it even contains a no-op `.replace('chevD','') && ...` short-circuit) — that is never called anywhere in the cluster. It is pure dead code / visual debt sitting at the top of the file.
  - fix: Delete line 8 (trendIc). If COACH-02 is accepted, also delete the spark helper (line 7) in the same pass.
  - file: `app/lib/scr-teacher.ts (line 8; and line 7 if COACH-02 lands)` · effort S · conf 0.9
- [COACH-09] **low** · content · _teacher_
  - problem: The third KPI tile is labeled 'Engagement promedio' but is fed k.onTime, and the source comment (lines 79-80) admits the value derives from Enrollment.engagement buckets (Alto/Medio/Bajo→%), NOT from on-time-vs-dueAt. The variable name onTime contradicts the visible label, and a coach could reasonably read 'Engagement %' as a punctuality/on-time-submission metric.
  - fix: Rename the data key to k.engagement (data layer) so label and field agree, OR add a one-line tooltip/subcaption on the tile clarifying it is the Alto/Medio/Bajo engagement average. Keep the visible label 'Engagement promedio'.
  - file: `app/lib/scr-teacher.ts (KPI line 80) + data source (DB.teacherKpis.onTime)` · effort S · conf 0.6
- [CW-07] **low** · eliminate · _coachwork › Disponibilidad / Ingresos_
  - problem: The escrow/take-rate message is repeated three+ times: (a) Ingresos tile sub 'se libera al completar' + 'después del take rate 18%', (b) the Ingresos info alert 'OTR retiene 18%… se libera vía escrow cuando marcas la sesión como completada', and (c) the Disponibilidad info alert 'Sesiones y pagos siempre dentro de OTR — los fondos quedan en escrow y se liberan al completar cada sesión'. The Disponibilidad escrow alert is content unrelated to managing time slots/packages and is pure reassurance noise on that tab.
  - fix: Remove the escrow info alert from viewAvailability() (it belongs in Ingresos), and de-duplicate: keep the single Ingresos 'Transparencia total' alert and drop the redundant take-rate restatement in the payout tile sub, or vice-versa. Target the noise, not the one canonical explanation.
  - file: `app/lib/scr-coachwork.ts › viewAvailability() escrow alert (lines ~375-377) + viewEarnings() tiles/alert (lines ~254-262)` · effort S · conf 0.7
- [CW-08] **low** · simplify · _coachwork › Agenda vs Ingresos_
  - problem: The same student renders with two different avatar identity colors depending on tab: Agenda rows force bg: var(--otr-navy) (black) on the avatar (bookingRow line ~186), while the Ingresos history table uses C.avatar with no bg, falling back to the default --otr-sky-lo green (avatar CSS line 211). A student seen as a black avatar in Agenda becomes a green avatar in Ingresos.
  - fix: Pick one avatar background for booking-student avatars and use it in both places — either drop the bg override in bookingRow() to match the table default, or add bg: var(--otr-navy) to the C.avatar call in viewEarnings()'s table row.
  - file: `app/lib/scr-coachwork.ts › bookingRow() avatar (line ~186) + viewEarnings() table avatar (line ~271)` · effort S · conf 0.8
- [PARENT-08] **low** · fix-bug · _parentPortal_
  - problem: The per-session 'Cancelar' button inside Próximas sesiones (childCard L181) uses a two-tap arm pattern with a 4s timeout (mount L516-528), but the destructive consequence is not explained: tapping once changes the label to '¿Cancelar? Tocar de nuevo' with no mention that cancellation may affect payment/escrow or notify the coach. Meanwhile the high-value Reject-approval button (consentRow L119) has NO confirmation at all.
  - fix: Align the two flows in scr-parent.ts: add a one-line consequence to the armed cancel state (e.g. '¿Cancelar? El pago se reembolsa') and apply the same lightweight 2-tap (or a small confirm) to the Reject button in consentRow/its handler so both destructive actions are consistent and explained.
  - file: `app/lib/scr-parent.ts (cancel handler L516-546; consentRow reject L119 + handler L487-511)` · effort M · conf 0.6
- [ADM-08] **low** · fix-bug · _adminUsers_
  - problem: Role change, coach verification, and suspension all fire immediately on change/click with only a success toast and no undo. A role <select> in particular is a single accidental change away from, e.g., promoting a student to Administrador, with no confirmation and no one-tap revert. The destructive 'Suspender al usuario' in adminConsole DOES get a two-tap confirm (ADM-04), so the two admin screens are inconsistent about how dangerous mutations are guarded.
  - fix: At minimum, add a confirm step for role changes that target or leave ADMIN (the highest-blast-radius transition); ideally add an 'Deshacer' action to the success toast that re-PATCHes the prior value. Align the guard model with adminConsole's two-tap pattern. File: app/lib/scr-admin-users.ts symbol patch + the [data-user-role] change handler (L260-266).
  - file: `app/lib/scr-admin-users.ts (patch L235-258, role handler L260-266)` · effort M · conf 0.66
- [ADM-09] **low** · simplify · _adminConsole_
  - problem: The moderation queue has no way to filter or distinguish OPEN vs REVIEWED vs by target type — every report renders as an equal-weight card in one flat list, and once 'Marcar revisado' is tapped the card stays in the list (only DISMISSED leaves). The header KPI says 'Reportes abiertos' but the list mixes open and reviewed with no segmentation, so a busy queue forces the admin to scan every card to find the actionable (OPEN) ones. Stripe/Linear-grade moderation tooling segments the actionable from the resolved.
  - fix: Add status filter chips ('Abiertos' / 'Revisados' / 'Todos') mirroring the role chips in adminUsers (reuse that pattern, with keyboard support per ADM-02), default to Abiertos, and/or sort OPEN before REVIEWED in viewBody. File: app/lib/scr-admin.ts symbols render() (L118-137) + viewBody (L91-115).
  - file: `app/lib/scr-admin.ts (render L118-137, viewBody L91-115)` · effort M · conf 0.62
- [PROF-06] **low** · content · _profile_
  - problem: In renderCoachSelf the two header buttons are 'Editar perfil' and 'Perfil de marketplace' (scr-profile.ts lines 216-217). The second label names a place, not an action, and it is unclear how 'marketplace profile' differs from the profile you are already editing — both open edit modals (openEditProfile vs openEditCoachMarketplace, Aula.tsx 931-932). A coach cannot predict which fields live behind which button.
  - fix: Relabel the second button to the concrete task, e.g. 'Editar oferta del marketplace' or 'Precios y disponibilidad' (whichever matches what openEditCoachMarketplace actually edits), in scr-profile.ts line 217. If the two modals overlap heavily, consider merging them into one tabbed edit modal (Tesler: absorb the split).
  - file: `app/lib/scr-profile.ts (renderCoachSelf marketplace button label line 217)` · effort S · conf 0.7
- [PROF-07] **low** · fix-bug · _profile_
  - problem: In renderStudentSelf the right-rail 'Tu rango' card prints `${(DB.xpNext - DB.xp)} XP para el siguiente nivel` (scr-profile.ts line 321) with raw number concatenation, while every other XP figure in the cluster uses `.toLocaleString('es')` (e.g. line 96, 98, 324). If xpNext-xp reaches four digits it renders '1234' here but '1.234' two lines below in the same card. There is also no guard for the max-level case (if xpNext===xp it shows '0 XP para el siguiente nivel' rather than a 'nivel máximo' message like S.progress handles at line 98).
  - fix: In scr-profile.ts line 321, format the delta with `.toLocaleString('es')` and reuse the max-level branch from S.progress (show '¡Nivel máximo!' when there is no next level / delta<=0) so the student-profile rail matches the progress screen.
  - file: `app/lib/scr-profile.ts (renderStudentSelf 'Tu rango' card line 321)` · effort S · conf 0.75
- [PROF-08] **low** · simplify · _profile_
  - problem: S.progress labels the competencies block 'Radar OTR' (scr-profile.ts line 102) but renders it as a vertical list of 6 horizontal bars (.comp-row, lines 107 / CSS screens.css 198-202), not a radar. The 6 fixed dimensions are exactly the kind of multi-axis comparison a radar/spider chart communicates at a glance; as a stacked bar list it reads as six unrelated metrics and the 'shape' of a debater's profile (the whole point of the OTR radar) is lost.
  - fix: Either rename the block to 'Competencias' and drop the 'Radar OTR' eyebrow (scr-profile.ts line 102) to stop over-promising, or implement an actual SVG radar for the 6 SKILL_DIMS. Cheapest correct fix: rename now, schedule the radar as a separate enhancement.
  - file: `app/lib/scr-profile.ts (S.progress 'Radar OTR' eyebrow + comp-row list lines 102-107)` · effort S · conf 0.65
- [SET-02] **low** · merge · _settings_
  - problem: Settings is built as 5 separate cards (Cuenta, Idioma, Notificaciones, Membresía, Privacidad) + a danger card, but three of the cards are single rows whose only content is a 'route elsewhere' button: 'Idioma' is one row, 'Membresía y facturación' is one row whose sole purpose is a 'Gestionar membresía →' button, and within Privacidad the 'Perfil público' row just routes to lifetime. Wrapping single routing rows in full titled cards inflates vertical length and dilutes the screen's real owned controls (language toggle, notif switches, leaderboard, password, logout).
  - fix: Merge 'Idioma' and 'Membresía' rows into the existing cards rather than giving each its own card: e.g. put the language control as a row inside the 'Cuenta' card, and fold 'Tu plan' into the same card or into Privacidad/seguridad as a routing row. Reserve full cards for sections that contain >1 owned control. This is a card-count reduction in S.settings.render, not a redesign.
  - file: `app/lib/scr-settings.ts (S.settings.render — card() calls for Idioma & Membresía)` · effort S · conf 0.65
- [SET-03] **low** · fix-bug · _settings_
  - problem: The 'Cambiar contraseña' control depends on a global w.otrFormModal; if it is absent the handler bails with toast 'No disponible aquí' (lines 158-159). That is a dead end with no fallback path to change a password — a security-relevant action silently becomes unavailable with an unhelpful message, and the user has no alternative route.
  - fix: In scr-settings.ts ensure otrFormModal is reliably available where settings renders, or provide a fallback (e.g. route to a dedicated password screen / inline form) instead of a terminal toast. At minimum make the message actionable ('Recarga la página e intenta de nuevo').
  - file: `app/lib/scr-settings.ts (change-pw handler, otrFormModal guard)` · effort S · conf 0.55
- [IA-05] **low** · merge · _Coach sidebar — 'Coaches' under 'Principal' group_
  - problem: The coach nav puts a lone 'Coaches' (explore/marketplace) item under group 'Principal' (shell.ts lines 48-50), then the real coach home (Panel de coach, Reservas, Gestionar, Mensajes, Mi perfil) under 'Espacio de coach'. A single-item 'Principal' group pointing at the consumer-side marketplace is an odd anchor — the coach's primary section is the workspace, so 'Principal' reads as the top/home group but contains a secondary, cross-side destination.
  - fix: In app/lib/shell.ts merge the lone 'explore' item into the 'Espacio de coach' group (e.g. as 'Mi perfil en marketplace' near 'Mi perfil') and drop the single-item 'group.main' for the teacher role, so 'Espacio de coach' becomes the unambiguous top section. Alternatively relabel it 'Marketplace' to match the student's grouping.
  - file: `app/lib/shell.ts (NAV.teacher, lines 47-71)` · effort S · conf 0.62
- [IA-06] **low** · eliminate · _Student sidebar — 'Niveles' item badge source_
  - problem: The student nav item for 'Niveles' declares a hardcoded badge:'Varsity' (shell.ts line 32), but navBadge() overrides it at runtime with DB.me?.level (shell.ts line 125), which per recent commit c1dcce4 is 'Novato' for new users. So the literal 'Varsity' string is dead source data — it never renders — but it is misleading to future maintainers and could resurface if the override logic changes.
  - fix: Remove `badge:'Varsity'` from the 'progress' nav item in app/lib/shell.ts line 32; the live badge is already sourced from DB.me.level in navBadge() (line 125), making the literal redundant and contradictory.
  - file: `app/lib/shell.ts (NAV.student 'progress' item, line 32)` · effort S · conf 0.8
- [DS-04] **low** · eliminate · _Global (CSS, affects no screen yet)_
  - problem: .btn-accent (the green affirmative-action button) is fully defined in app.css:134-135 with hover, but is used 0 times in any of the 21 scr-*.ts builders — confirmed: the only 3 references to 'btn-accent' in the whole repo are the CSS definition and a comment. Affirmative CTAs (Continuar/Confirmar/Reservar/Guardar) all correctly use btn-primary (black) per the brand book.
  - fix: Either (a) delete .btn-accent and its hover from app.css:134-135 to remove the trap, or (b) if green affirmative CTAs are intended for a specific case (e.g. 'Confirmar pago'), document that single allowed use in a comment and adopt it consistently. Given the brand book explicitly says primary=black, prefer deletion.
  - file: `app/styles/app.css:134-135 (.btn-accent)` · effort S · conf 0.85
- [DS-05] **low** · merge · _Topbar (shell) + Settings_
  - problem: The ES/EN language pill toggle is implemented twice with copy-pasted inline styles: shell.ts:177-178 (topbar) and scr-settings.ts:76-78. Both are border-radius:100px containers with navy 'on' / transparent buttons and nearly identical padding/font rules, maintained independently.
  - fix: Extract a langToggle(lang) helper (or a .seg-pill class) and use it in both shell.ts:177 and scr-settings.ts:76. While there, replace the inline border-radius:100px with var(--r-pill) for token consistency.
  - file: `app/lib/shell.ts:177-178 + app/lib/scr-settings.ts:76-78` · effort S · conf 0.8
- [DS-06] **low** · simplify · _Global (pill-shaped controls)_
  - problem: Pill radius is written as a literal (border-radius:100px or 999px) inline in 6+ spots instead of var(--r-pill): scr-lifetime.ts:372, scr-placement.ts:72, scr-settings.ts:30/37/76/78, shell.ts:177/178. tokens.css already defines --r-pill:999px specifically for this.
  - fix: Replace the literal border-radius:100px / 999px with var(--r-pill) in the listed inline styles. Best folded into DS-02 and DS-05 since those controls are being componentized anyway.
  - file: `app/lib/scr-settings.ts:30,37,76,78 + scr-lifetime.ts:372 + scr-placement.ts:72 + shell.ts:177,178` · effort S · conf 0.82
- [A11Y-05] **low** · a11y · _All create/edit modals with a rich-text field (Course Builder, lesson/resource editors)_
  - problem: In formModal (Aula.tsx:161-167) the rich-text field's <label for="${fid}"> targets a contenteditable <div id="${fid}" data-rich="1">. A native <label for> only binds to labelable form controls — it does not associate with, nor click-focus, a contenteditable div. The div also lacks role="textbox" and an accessible name, and the formatting toolbar buttons rely on title="" (not aria-label) for their names.
  - fix: On the contenteditable div add role="textbox" aria-multiline="true" and aria-labelledby pointing at the label (give the <label> an id), instead of relying on for/id. Optionally wire the label's click to focus the editor. Keep title on toolbar buttons but add matching aria-label for SR exposure.
  - file: `app/components/Aula.tsx (formModal richtext branch ~L161-167)` · effort S · conf 0.8
- [SIMP-06] **low** · simplify · _Profile (student self)_
  - problem: renderStudentSelf (scr-profile.ts:270-336) rebuilds the level/XP/streak panel ('Tu rango' card, lines 316-325) and the highlighted-badges card (lines 326-333) — both are smaller copies of what progress and badges already show, and once SIMP-01 lands, of what lifetimeProfile will show. The student profile thus becomes a fourth place to see level+XP+streak+badges.
  - fix: Trim the student profile right rail to identity + enrolled programs, and replace the duplicated 'Tu rango' and 'Insignias destacadas' cards with a single 'Ver mi trayectoria' link to lifetime. Keeps profile focused on who-you-are; progress numbers live in one place.
  - file: `app/lib/scr-profile.ts (renderStudentSelf right-rail, lines 316-333)` · effort S · conf 0.62
- [SIMP-07] **low** · merge · _Parent Portal_
  - problem: The single-column parentPortal stacks a lot for one screen: a 4-KPI strip, a pending-approvals card, per-child cards (each with skills + attendance + achievements + upcoming), then a right rail with Seguridad+consentimiento card, public-profile per-child toggles, a Mensajes card, a Membresía+per-child-threshold card (membershipCard), a monthly-report card, AND a 'Vincular otro estudiante' card. The right rail alone is six stacked cards. Two of them (public-profile consent in the security card, and approval-threshold in the membership card) are both privacy/consent controls split across different cards.
  - fix: Merge the per-child public-profile toggles and the per-child approval-threshold selector into a single 'Permisos y consentimiento por hijo' card (one row per child with both controls), and collapse the static 3-bullet 'Seguridad' reassurance into a short footnote rather than a full card. Cuts the right rail from 6 cards to ~4 and co-locates the consent decisions.
  - file: `app/lib/scr-parent.ts (membershipCard thresholds lines 330-353 + security/public-profile block lines 428-450)` · effort M · conf 0.58
- [SIMP-08] **low** · simplify · _Membership_
  - problem: The membership pricing grid always renders a third 'Elite' tier card (scr-lifetime.ts:544-556) whose every CTA is disabled 'Próximamente' and whose price is 'Muy pronto'. It occupies a full third of the pricing row purely to advertise something nobody can buy, splitting visual attention away from the one upgrade that exists (Pro).
  - fix: Replace the full Elite card with a single slim 'OTR Elite — próximamente' teaser strip below the Free/Pro pair (hide-until-purchasable). Let the pricing grid be a focused 2-up Free vs Pro comparison so the Pro CTA dominates.
  - file: `app/lib/scr-lifetime.ts (tierCards eliteCard, lines 544-558)` · effort S · conf 0.6
- [SIMP-09] **low** · eliminate · _Lesson (legacy demo prose path)_
  - problem: S.lesson carries a 10-line hardcoded 'defaultProse' Claim/Warrant/Impact essay (scr-learn.ts:551-560 and again referenced in scr-core.ts:551-571) rendered only when no lesson is selected (legacy entry without window.__lesson). In a real multi-course app the lesson screen is always reached with a selected lesson, so this fallback ships demo content that can only appear via an unreachable/legacy path — and it duplicates the same prose block in two files (scr-core.ts and scr-learn.ts both define S.lesson with the prose).
  - fix: Replace the defaultProse fallback with the honest empty state already used elsewhere ('Selecciona una lección de tu curso' → go('course')), and delete the duplicate S.lesson definition so a single builder owns the screen. Removes ~12 lines of dead demo content and one duplicate.
  - file: `app/lib/scr-learn.ts (S.lesson defaultProse, lines 551-571) — reconcile with duplicate in app/lib/scr-core.ts S.lesson (lines 541-616)` · effort S · conf 0.55
- [COG-05] **low** · simplify · _Debate Hub (debateHub)_
  - problem: The hub exposes 6 sub-tabs (Resumen, Mis debates, Práctica, Leaderboard, Torneos, Analytics). Six is within Miller's 7±2 but at the top of it, and the navy hero already shows rating + recent form + 'Registrar un debate', which the 'Resumen' tab largely re-presents — so the first thing the user sees partly duplicates the first tab.
  - fix: Leave the 6 tabs (each is a real destination) but de-duplicate: since the hero already carries rating/form/record-CTA, trim the Overview tab to the NON-hero content (e.g., next steps + quick analytics teaser) rather than restating rating/form. No tab removal needed; this is content reduction, not redesign.
  - file: `app/lib/scr-debate.ts (TABS array lines 27-34; heroPanel ~113-150; Overview section after line 160)` · effort S · conf 0.55

