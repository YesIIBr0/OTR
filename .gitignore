node_modules
.next
out
.env
.env*.local
.env.production
.env.development
# (se conservan los *.example para plantilla)
*.log
.DS_Store

# Prisma / SQLite — artefactos de desarrollo (la BD real es Postgres en producción)
*.db
*.db-journal
*.sqlite

# misc
*.tsbuildinfo

# material entrante (staging de exports — no commitear)
_incoming/

# archivos subidos por usuarios (se conservan en el servidor/volumen, no en git)
public/uploads/*
!public/uploads/.gitkeep
var/uploads/
var/
